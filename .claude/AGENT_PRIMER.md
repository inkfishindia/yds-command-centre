# Agent Primer — YDS Command Centre

> **Generated:** 2026-04-23T15:21:58.849Z · **Commit:** `36064ee` · **Auto-generated by `scripts/generate-agent-primer.mjs`** — do not edit by hand.

This file is the single context document sub-agents read before doing work. It fuses the stable architecture (from `CLAUDE.md`), the live app inventory (from the System Map service), and the last three session handoffs. If anything in here is wrong, regenerate with `npm run agent-primer`.

---

## 1. Architecture (from CLAUDE.md)

## Stack

Node.js + Express · Alpine.js SPA · Anthropic Claude SDK (Opus) · Notion SDK · SSE · Lightweight asset build (`npm run build`)

## Architecture

```
Browser (Alpine.js SPA)
        |
        | static shell + JSON/SSE
        v
Express Server (server.js)
        |
        +-- Routes (controller-thin HTTP layer)
        |
        +-- Domain services (dashboard, projects, CRM, marketing ops, tech team, factory)
        |
        +-- Infrastructure services (Notion, Sheets, hydration, GitHub, approvals, agent loop)
        |
        +-- Tool handler / approval gate
        |
        +-- Claude API
```

## File Map

```
server.js                          # Express entry, middleware, static serving
server/config.js                   # Env vars, model config, workspace paths
server/routes/
  chat.js                          # POST /api/chat (SSE stream), approval resolve
  notion.js                        # Notion-backed read routes
  commitments.js                   # Commitment write routes
  decisions.js                     # Decision write routes
  crm.js                           # CRM endpoints
  marketing-ops.js                 # Marketing operations endpoints
  tech-team.js                     # Tech team endpoints
  factory.js                       # Factory config + simulation endpoints
  bmc.js                           # Business Model Canvas endpoints
  registry.js                      # Internal project registry endpoints
  sheets.js                        # Registered Google Sheets access
  documents.js                     # Briefings, decisions, weekly reviews
  skills.js                        # List available skill buttons
server/services/
  agent.js                         # Claude API agentic loop (tool use cycle)
  notion.js                        # Notion SDK wrapper, 5-min cache, raw query primitives
  dashboard-service.js             # Dashboard summary + action queue composition
  projects-service.js              # Project enrichment + commitment stats
  notion-detail-service.js         # Focus area + person detail composition
  crm-service.js                   # CRM aggregation/filtering
  marketing-ops-service.js         # Marketing Ops aggregation/filtering
  tech-team-service.js             # Tech Team aggregation/catalog composition
  factory-service.js               # Factory config read/write orchestration
  sheets.js                        # Google Sheets access + cache
  hydration.js                     # FK hydration between sheet datasets
  github.js                        # GitHub repo activity wrapper
  prompts.js                       # Load system prompt from Colin's workspace
  approval.js                      # Promise-based write approval queue
server/tools/
  notion-tools.js                  # Tool schemas: query, get, create, update
  file-tools.js                    # Tool schemas: read, write, list
  tool-handler.js                  # Central dispatch + Notion property converter
public/
  index.html                       # SPA shell (Alpine.js templates)
  js/app.js                        # Built frontend bundle copied from src/js
  css/styles.css                   # Dark theme, responsive layout
src/js/
  app.js                           # Root shell + shared utilities + module composition
  modules/                         # Domain frontend modules (chat, dashboard, commitments, factory, command-shell, CRM, etc.)
.claude/
  agents/                          # Agent configs (frontend-builder, backend-builder, code-reviewer, devops-infra, ux-auditor, pixel, design-planner, tester, scribe)
  rules/                           # Coding patterns (frontend, server, token efficiency, api-schemas, workflow)
  skills/ui-ux-pro-max/            # Design intelligence: 50+ styles, 97 palettes, 57 font pairings, UX guidelines
  docs/app-reference.md            # FULL APP INVENTORY — routes, views, state, methods, CSS, tools. Read before building.
  docs/notion-hub.md               # Notion database IDs, people IDs, property schemas, write templates
  docs/tech-brief.md               # Technical architecture brief
data/schemas/                      # Captured API response samples (Notion/Sheets/GitHub). Gitignored; regenerate via scripts/capture-schemas.js.
scripts/capture-schemas.js         # One-shot: calls live services, saves real response shapes to data/schemas/.
```

## Critical Patterns

1. **Approval Gate** — All writes pause, send SSE `approval` event, wait for Dan. Never bypass.
2. **Agent Loop** — Claude responds → tool calls → execute/approve → feed back → repeat until `end_turn`.
3. **Thin Routes** — Express routes validate/request-shape/HTTP concerns; business orchestration belongs in domain services.
4. **Notion + Sheets Infrastructure** — raw SDK/data access stays in infrastructure services; domain services compose product-facing payloads.
5. **Frontend Modules** — keep domain state in `src/js/modules/*`; `src/js/app.js` should remain a shell/composition layer, not a dumping ground.
6. **Build Step Exists** — edit `src/js/*`, then build to `public/js/*`.

## Agent Routing

| Task | Agent | Model | Owns |
|------|-------|-------|------|
| Design planning | `design-planner` | Haiku | `design-system/` |
| Frontend code | `frontend-builder` | Sonnet | `src/js/`, `public/`, `css/` |
| Backend code | `backend-builder` | Sonnet | `server/`, `test/`, `server.js` |
| Quality gate | `code-reviewer` | Haiku | read-only review |
| UX consistency | `ux-auditor` | Haiku | read-only audit |
| Image generation | `pixel` | Sonnet | `~/Documents/nanobanana_generated/` |
| Deploy/infra | `devops-infra` | Haiku | config files |
| Integration tests | `tester` | Haiku | `test/integration/` |
| Documentation | `scribe` | Haiku | `.claude/docs/` |

**File ownership is exclusive** — only one builder agent writes to each file area. This prevents conflicts and keeps context focused.

## Rules

- Never modify `../dan/` (Colin's workspace) — read only
- Never bypass approval gate for writes
- CommonJS only on server (`require`/`module.exports`)
- Alpine.js only on frontend (no React)
- Frontend source lives in `src/js`; built assets live in `public/js`
- Keep route handlers thin; move aggregation/enrichment into domain services
- Raw Notion access goes through `server/services/notion.js`; route/view payload composition goes in domain services
- Dates from Notion are `{start, end}` objects — handle in templates

---

## 2. Live App Inventory

### 2.1 API Routes

### `activity-feed.js`
- **GET** `/` —   days  — look-back window, 1–90 (default 14)

### `ai-team.js`
- **GET** `/` — GET /api/ai-team — full AI team roster with resolved relations

### `bmc.js`
- **GET** `/` — Maps to the classic Business Model Canvas 9-block layout.
- **GET** `/:section` — Returns a single BMC section, hydrated.

### `ceo-dashboard.js`
- **GET** `/`
- **POST** `/forge`
- **POST** `/review`
- **POST** `/brief`

### `chat.js`
- **POST** `/` — Response: Server-Sent Events stream
- **POST** `/approve` — Body: { approvalId: string, approved: boolean }
- **POST** `/clear` — Clear conversation history and start fresh.
- **GET** `/pending` — Get any pending approvals.

### `commitments.js`
- **POST** `/`
- **PATCH** `/:pageId/status`
- **PATCH** `/:pageId/priority`
- **PATCH** `/:pageId/due-date`
- **PATCH** `/:pageId/assignee`
- **PATCH** `/:pageId/notes`

### `competitor-intel.js`
- **GET** `/` — Overview: competitor list, tier/category breakdown, recent notes, watchlist, capability summary
- **GET** `/competitor/:brand` — Full merged profile for a single competitor across all 12 sheets
- **GET** `/capabilities` — All capability rows for radar/comparison view
- **GET** `/swot` — All SWOT rows
- **GET** `/watchlist` — Query: type (Change_Type), days (recent N days)
- **GET** `/steal-adapt` — Steal/adapt/avoid rows grouped by Action

### `crm.js`
- **GET** `/` — Aggregated CRM overview — pipeline stats, lead stats, team, flow stats
- **GET** `/leads` — Query: status, category, search, page, limit
- **GET** `/leads/:id` — Single lead with all associated flows
- **GET** `/flows` — Query: status, stage, owner, channel, priority, search
- **GET** `/flows/:id` — Single flow with lead, details, and suggested actions
- **GET** `/team` — System users with flow count stats
- **GET** `/config` — CRM config tables: SLA rules, routing rules, flow type config, message templates

### `dan-colin.js`
- **GET** `/` — ────────────────────────────────────────────────────────────────────
- **POST** `/:id/answer` — ────────────────────────────────────────────────────────────────────
- **POST** `/drop` — ────────────────────────────────────────────────────────────────────

### `decisions.js`
- **POST** `/`

### `documents.js`
- **GET** `/` — List all documents across all directories.
- **GET** `/:category` — List documents in a specific category (briefings, decisions, weekly-reviews).
- **GET** `/:category/:filename` — Read a specific document.
- **POST** `/review`

### `factory.js`
- **GET** `/config` — Returns the full factory config (machines, zones, order_mix, operating, formulas).
- **PUT** `/config` — Body: full config object (machines, zones, order_mix, operating).
- **POST** `/machines` — Body: machine object.
- **PUT** `/machines/:id` — Body: partial or full machine object (id field is preserved from the URL param).
- **DELETE** `/machines/:id` — Remove a machine by id.
- **PUT** `/zones/:id` — Body: partial or full zone object (id field is preserved from the URL param).
- **PUT** `/operating` — Body: operating object (shift_hours, orders_per_day, etc.).

### `health.js`
- **GET** `/`
- **GET** `/details`
- **POST** `/sync`
- **POST** `/sync/:name`

### `marketing-ops.js`
- **GET** `/` — GET /api/marketing-ops — aggregated summary
- **GET** `/campaigns` — GET /api/marketing-ops/campaigns
- **GET** `/content/calendar` — GET /api/marketing-ops/content/calendar?month=YYYY-MM
- **GET** `/content` — GET /api/marketing-ops/content
- **POST** `/content` — POST /api/marketing-ops/content — create a new content calendar item
- **PATCH** `/content/:id` — PATCH /api/marketing-ops/content/:id — update a content calendar item
- **GET** `/sequences` — GET /api/marketing-ops/sequences
- **GET** `/sessions` — GET /api/marketing-ops/sessions
- **GET** `/tasks` — GET /api/marketing-ops/tasks — marketing tasks with optional ?status=&priority=&channel= filters
- **GET** `/tasks/summary` — GET /api/marketing-ops/tasks/summary — aggregated stats across all marketing tasks
- **PATCH** `/campaigns/:id` — PATCH /api/marketing-ops/campaigns/:id — update campaign property (requires approval)
- **GET** `/campaigns/:id/commitments` — GET /api/marketing-ops/campaigns/:id/commitments — commitments linked to a campaign
- **GET** `/metrics` — GET /api/marketing-ops/metrics — key business metrics

### `notebooks.js`
- **GET** `/` — GET /api/notebooks — full parsed registry
- **POST** `/cache/clear` — POST /api/notebooks/cache/clear

### `notion.js`
- **GET** `/morning-brief` — Operational brief derived from dashboard data: overdue, today's items, top priorities, flags.
- **GET** `/dashboard` — Full dashboard summary — focus areas, overdue commitments, recent decisions, people.
- **GET** `/focus-areas` — GET /api/notion/focus-areas
- **GET** `/commitments/all` — Filtering is performed client-side — this route returns the full unfiltered list.
- **GET** `/commitments/overdue` — GET /api/notion/commitments/overdue
- **GET** `/commitments/upcoming` — GET /api/notion/commitments/upcoming
- **GET** `/decisions` — GET /api/notion/decisions
- **GET** `/people` — GET /api/notion/people
- **GET** `/projects` — and commitment stats (openCount, overdueCount, doneCount, totalCount, progressPercent)
- **GET** `/databases` — List all known databases
- **GET** `/databases/:id` — Query a specific database
- **GET** `/pages/:id` — Get a page's properties
- **GET** `/pages/:id/related` — Resolve relation properties to page summaries
- **GET** `/pages/:id/content` — Get a page's block content as markdown
- **GET** `/key-pages` — List key Notion pages
- **GET** `/focus-areas/:id/detail` — Full view of one focus area: properties, linked projects, commitments, decisions, stats.
- **GET** `/people/:id/detail` — Full view of one person: properties, commitments assigned to them, load metrics.
- **GET** `/team-workload` — Returns [{ id, name, activeCount, overdueCount, capacity }]
- **GET** `/recent-activity` — Returns { completions, newBlockers, recentDecisions }
- **GET** `/action-queue` — Both lists are sorted by severity (overdue first, then by days overdue desc).
- **POST** `/cache/clear` — POST /api/notion/cache/clear

### `ops.js`
- **GET** `/` — Aggregated ops overview — stock health, alerts, pending POs, vendors, products
- **GET** `/stock` — Query: status, product, vendor, search, tier
- **GET** `/sales` — Aggregated sales overview — totals, by channel, by status, trends, top products
- **GET** `/products` — Query: status, vendor, tier, search
- **GET** `/purchase-orders` — All purchase orders with computed urgency

### `overview.js`
- **GET** `/`

### `read-models.js`
- **GET** `/`
- **GET** `/:name`

### `registry.js`
- **GET** `/` — GET /api/registry — all projects sorted by priority desc
- **GET** `/:slug` — GET /api/registry/:slug — single project
- **PATCH** `/:slug` — PATCH /api/registry/:slug — update a project field

### `sheets.js`
- **GET** `/pipeline` — must handle this gracefully and not show an error to Dan.
- **GET** `/:sheetKey` — Returns { available: false } when the spreadsheet is not configured.
- **POST** `/:sheetKey` — Body: JSON object with column names as keys.
- **PATCH** `/:sheetKey/:rowIdx` — Body: JSON object with column names as keys.
- **DELETE** `/:sheetKey/:rowIdx` — Delete a specific row (rowIdx is 1-based, >= 2).

### `skills.js`
- **GET** `/` — List available skills with metadata.

### `system-map.js`
- **GET** `/` — Cached 60s server-side. Pass ?force=true to bust the cache.

### `tech-team.js`
- **GET** `/` — GET /api/tech-team — Tech team summary (command center data)
- **GET** `/sprint` — GET /api/tech-team/sprint — Sprint board items with optional filters
- **GET** `/bugs` — GET /api/tech-team/bugs — Sprint items filtered to Type=Bug
- **GET** `/specs` — GET /api/tech-team/specs — Spec pipeline
- **GET** `/decisions` — GET /api/tech-team/decisions — Tech decision log
- **GET** `/velocity` — GET /api/tech-team/velocity — Sprint archive for velocity charts
- **GET** `/agents` — GET /api/tech-team/agents — Agent registry + skill catalog from file system
- **GET** `/strategy` — GET /api/tech-team/strategy — Strategy cascade from Google Sheets
- **GET** `/github` — GET /api/tech-team/github — GitHub repo activity
- **GET** `/backlog` — GET /api/tech-team/backlog — Tech backlog with optional ?status=&priority=&area=&type= filters
- **PATCH** `/sprint/:id` — PATCH /api/tech-team/sprint/:id — Update sprint item property (direct user write)

### 2.2 Frontend Modules (`src/js/modules/`)

- **bmc** → view `bmc` — exports: `createBmcModule`
- **chat** → view `chat` — exports: `createChatModule`
- **claude-usage** → view `claude-usage` — exports: `createClaudeUsageModule`
- **command-shell** — exports: `createCommandShellModule`
- **commitments** → view `commitments` — exports: `createCommitmentsModule`
- **competitor-intel** — exports: `createCompetitorIntelModule`
- **crm** → view `crm` — exports: `createCrmModule`
- **dan-colin** → view `dan-colin` — exports: `createDanColinModule`
- **dashboard** → view `dashboard` — exports: `createDashboardModule`
- **detail-drawer** → view `detail-drawer` — exports: `createDetailDrawerModule`
- **documents** — exports: `createDocumentsModule`
- **factory** → view `factory` — exports: `createFactoryModule`
- **inline-actions** → view `inline-actions` — exports: `createInlineActionsModule`
- **markdown** — exports: `configureMarkdown`
- **marketing-ops** — exports: `createMarketingOpsModule`
- **notion-browser** — exports: `createNotionBrowserModule`
- **ops** → view `ops` — exports: `createOpsModule`
- **overview** → view `overview` — exports: `createOverviewModule`
- **projects** → view `projects` — exports: `createProjectsModule`
- **read-models** — exports: `unwrapReadModelResponse`, `getReadModelUrl`, `formatReadModelFreshness`, `getReadModelTone`, `getReadModelSummary`
- **registry** → view `registry` — exports: `createRegistryModule`
- **system-map** → view `system-map` — exports: `createSystemMapModule`
- **system-status** — exports: `createSystemStatusModule`
- **team** → view `team` — exports: `createTeamModule`
- **tech-team** — exports: `createTechTeamModule`
- **toasts** → view `toasts` — exports: `createToastsModule`

### 2.3 Notion Databases

- **Focus Areas** `274fc2b3…` — Health checks, strategic tracking
- **Projects** `85c1b292…` — Mission briefs, initiative tracking
- **Commitments** `0b50073e…` — Task tracking, accountability
- **People** `de346469…` — Assignment, routing
- **Decisions** `3c8a9b22…` — Decision logging
- **Platforms** `1fcf264f…` — System tracking
- **Audiences** `63ec25ca…` — Segment targeting
- **Campaigns** `9f5f3da6…` — Marketing campaign tracking
- **Content Calendar** `227f3365…` — Content scheduling & status
- **Sequences** `e580d12c…` — Email/automation sequences
- **Sessions Log** `dffaf6eb…` — Session tracking & analytics
- **Sprint Board (Tech)** `2c459dc9…` — Tech sprint items, bugs, tasks · props: Name (title), Type (select), Status (select), Priority (select), System (select), Sprint (relation)…
- **Spec Library** `5be6d7cf…` — Technical specification pipeline · props: Name (title), Status (select), Type (select), Owner (relation), Related Sprint Item (relation), Summary (text)…
- **Tech Decision Log** `1f9193d4…` — Technical decision records · props: Name (title), Date (date), Status (select), Owner (relation), Context (text), Decision (text)…
- **Sprint Archive** `9ba8330a…` — Sprint velocity history · props: Sprint Number (number), Start Date (date), End Date (date), Planned Items (number), Completed Items (number), Velocity (number)…
- **AI Team** `17f15cb3…` — AI agent roster & capabilities
- **Marketing Tasks** `1fa22f26…` — Marketing action items & assignments · props: Task (title), Status (select), Priority (select), Type (select), Channel (select), Assigned To (person)…
- **Tech Backlog** `4bb401d8…` — Tech backlog items

### 2.4 Google Sheets (presence only — IDs never exposed)

- ✓ `GOOGLE_SHEETS_ID` — Google Sheets (legacy)
- ✗ `STRATEGY_SPREADSHEET_ID` — Strategy
- ✓ `EXECUTION_SPREADSHEET_ID` — Execution
- ✓ `APP_LOGGING_SPREADSHEET_ID` — App Logging
- ✓ `BMC_SPREADSHEET_ID` — Bmc
- ✓ `CRM_CONFIG_SPREADSHEET_ID` — Crm Config
- ✓ `CRM_FLOWS_SPREADSHEET_ID` — Crm Flows
- ✓ `OPS_INVENTORY_SPREADSHEET_ID` — Ops Inventory
- ✓ `OPS_SALES_SPREADSHEET_ID` — Ops Sales
- ✓ `OPS_PRODUCTS_SPREADSHEET_ID` — Ops Products
- ✓ `OPS_WAREHOUSE_SPREADSHEET_ID` — Ops Warehouse
- ✓ `COMPETITOR_INTEL_SPREADSHEET_ID` — Competitor Intel

### 2.5 Agents

- **backend-builder** — Backend engineering agent for YDS Command Centre. Use PROACTIVELY for Express routes, Notion service…
- **code-reviewer** — Code Reviewer and Quality Gate for YDS Command Centre. Use PROACTIVELY after any code…
- **design-planner** — Proactive design system planner for YDS Command Centre. Use BEFORE building UI — generates…
- **devops-infra** — DevOps and Infrastructure specialist for YDS Command Centre. Use for deployment configuration, environment management,…
- **frontend-builder** — Frontend engineering agent for YDS Command Centre. Use PROACTIVELY for all Alpine.js template changes,…
- **pixel** — Image generation specialist. Use when ANY agent or user needs AI-generated images, hero graphics,…
- **scribe** — Documentation agent for YDS Command Centre. Use PROACTIVELY after code-reviewer approves any code change…
- **tester** — Integration test agent for YDS Command Centre. Use PROACTIVELY after code-reviewer approves changes to…
- **ux-auditor** — UX consistency and quality auditor for YDS Command Centre. Use after UI changes to…

---

## 3. Recent Work (last 3 session handoffs)

## 2026-04-23 07:45
**Accomplished:** Dan attempted manual test of Dan↔Colin Queue and hit "Failed to load queue." Three-stage diagnosis: (1) stale dev server (started 00:26, before the route was added at line 130 of server.js) returning SPA fallback for `/api/dan-colin` — fixed by restart. (2) After restart, endpoint returned `200 { counts: {total: 0} }` with empty arrays — Notion error was being silently swallowed by `server/services/notion.js:983-986` (`queryDatabase` catches all errors and returns `{ results: [] }` — a latent bug worth fixing separately). Direct curl to Notion API exposed the real error: `object_not_found` for DB `00969f07-8b4d-4c88-8a45-ec1e95b3bacb`, integration `Rube`. (3) After Dan connected Rube to what he thought was the right DB, still 404 — Notion `/v1/search` revealed the actual "Dan ↔ Colin Queue" DB ID is `43d71386-85a1-4582-a2dd-6d541bdcc5d3`, not `00969f07...`. The original ID yesterday's build used was mis-transcribed at spec time — schema matched perfectly but the ID pointed at a non-existent DB. Fixed hardcoded DB_ID in `server/services/dan-colin-service.js:5` (comment) + `:21` (constant), updated assertion in `test/dan-colin-service.test.js:377` to match. After final restart: `/api/dan-colin` returns 20 real rows (now: 2 · waiting: 4 · drop: 0 · watch: 7 · closed: 7).
**Decisions:** One new — see #31 in decisions.md.
**Next:** (a) Dan to reload `/dan-colin` and complete manual UI test (approval flow on answer, drop sheet UX, mobile vs desktop layout). (b) `scribe` pass: `.claude/docs/app-reference.md` still references the wrong DB ID `00969f07` in two places (lines 5 + 126) — trivial fix but scribe-owned. (c) Worth patching `server/services/notion.js:983-986` so `queryDatabase` doesn't silently swallow 403/404 — should propagate as thrown error, let caller decide. This cost us ~20 minutes of diagnosis today. (d) Update `.claude/rules/api-schemas.md` to require BOTH schema verification AND a live row-query when hardcoding a DB ID, not just schema fetch — would have caught this at build time.

## 2026-04-23 14:12
**Accomplished:** Fixed tab-navigation bug — switching to chat / dashboard / actionQueue / decisions / knowledge rendered empty containers. Root cause: 12 nav buttons in `public/index.html` (5 desktop sidebar + 7 mobile bottom-nav/drawer) used the legacy `view = 'X'; loadX()` pattern, bypassing `openNavigationTarget()` which is the only path that calls `_loadPartial()` to inject partial HTML and `_ensureModule()` to init the lazy module. Replaced all 12 with `openNavigationTarget('X')`; verified zero `@click="view = ` remain. Confirmed dev server (pid 89467) serves the patched HTML — `openNavigationTarget('chat')` appears 3× in served response.
**Decisions:** none
**Next:** (a) Dan hard-refreshes (Cmd+Shift+R) and clicks each affected tab to confirm. (b) Same bypass pattern lives in command-palette "New Commitment" / "New Decision" actions at [src/js/modules/command-shell.js:233-235](src/js/modules/command-shell.js#L233) — not currently breaking (commitments view typically loaded), but worth a follow-up sweep. (c) All carry-overs from prior sessions still pending (notion.js silent-swallow, api-schemas tightening, Dan↔Colin v2 scribe/tester, dashboard manual test, lint cleanup in 3 test files).

---

## 2026-04-23 15:12
**Accomplished:** Scanned ERP app at `~/Downloads/copy-of-your-design-lab_-erp`, picked 3 features to port. #1 shipped: System Map now has a "Views & Status" section — parses `public/index.html` for all `view === 'X'` expressions, cross-references a 24-entry `VIEW_REGISTRY` in `server/services/system-map-service.js`, returns `views[]` with `functional | beta | mock | unknown` status + file-existence checks (`hasPartial`, `hasModule`). Frontend renders collapsible card grid with status pills, search-filter, count badge. +5 backend tests, 13/13 system-map tests pass. `npm run build` clean. #2 (UTM Builder in Marketing Ops) and #3 (Activity Feed on Dashboard backend) launched as background builder agents (af4ec43914987e316, af52ea4c187317b0b) — not yet returned.
**Decisions:** none — scoping choices only (skip orders/production/AI-studio/workspace integrations from ERP as out-of-scope; skip design-planner + ux-auditor + tester per standing instruction).
**Next:** Wait for background builder agents. Then fire frontend-builder for #3 dashboard feed integration (needs backend endpoint shape), then code-reviewer across #2+#3. Dan to visually test #1 now.

---

_End of primer. For full details: `.claude/docs/app-reference.md`, `.claude/docs/notion-hub.md`, `.claude/docs/tech-brief.md`._
