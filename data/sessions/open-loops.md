# Open Loops

Work started but not completed. SessionStart surfaces these first.

| Date | Loop | Context | Status | Age |
|------|------|---------|--------|-----|
| 2026-05-02 | EA briefing delivered — awaiting Dan's pick on destination format | Built 5-section structured brief (nav groups + data links + patterns + repo layout + current focus) from AGENT_PRIMER.md + index.html grep. Read-only, no code changes. Closed with format question: PDF / Notion page / Google Doc / non-technical trim. Once Dan picks, render to chosen format (most likely Notion page or Google Doc for the EA's day-to-day reference) | open | 0d |
| 2026-05-02 | /mcc 3-bug wiring fix shipped — Dan visual verification + reviewer dispatch pending | command-shell.js:74 ('mcc' added to partialViews) + command-shell.js:130 (initMcc() branch added) + mcc.html (39 mccModule.X refs → direct access). Build clean, 1014/1014 tests, served partial has 0 mccModule refs. After Dan confirms /mcc renders (OAuth bar + 5-col kanban + empty states + "+ New Post" header), dispatch code-reviewer + ux-auditor in parallel. Reviewer should specifically check: lazy-module merge-pattern compliance, the truthiness guard issue noted below, the unrequested emoji-entity cleanup | open | 0d |
| 2026-05-02 | mcc.html x-if truthiness guards — loading skeleton invisible (deferred tweak) | At [public/partials/mcc.html:36](public/partials/mcc.html#L36) and :43, agent rewrote always-falsy `x-if="mccModule"` to `x-if="mccLoading && !mccPosts"` / `x-if="!mccLoading \|\| mccPosts"`. But mccPosts initializes to `[]` — empty array is truthy in JS, so `!mccPosts` is always false → the loading-skeleton template **never** shows. Kanban always renders (functionally fine because per-column empty states at line 76 cover zero-post case). Fix: change both to `.length`-based checks (`!mccPosts.length` / `mccPosts.length > 0`). Low priority — UX polish only, no functional break | open | 0d |
| 2026-05-02 | mcc.html unrequested emoji-to-entity cleanup — brief discipline drift (no action needed) | Frontend-builder converted 🚀 → `&#x1F680;` and 🗑️ → `&#x1F5D1;` at [public/partials/mcc.html:68](public/partials/mcc.html#L68) and :71 despite brief saying no "while I'm here" cleanups. Harmless — entities render as identical glyphs. Logging for code-reviewer awareness; no remediation needed | open | 0d |
| 2026-05-02 | src/css/views/mcc.css created (486 lines) — Dan visual verification + reviewer dispatch pending | Fixed runtime CSS 404 (view registered in app.js:127 but file didn't exist). Built output 6,917 bytes. Zero collisions for the 6 form primitives across src/css/. 1014/1014 tests, lint clean, inline-style baseline in mcc.html = 0. After Dan hard-refresh confirms /mcc renders styled (pipeline kanban colored borders, IG pink/LI blue badges, 2→1 col composer at 768px, wide modal), dispatch code-reviewer + ux-auditor in parallel | open | 0d |
| 2026-05-02 | mcc.css form-primitive migration to core.css — deferred until 2nd composer-style view lands | 6 generic form classes (`.form-group`, `.form-input`, `.form-textarea`, `.form-select`, `.form-actions`, `.checkbox-label`) shipped in src/css/views/mcc.css because they have no existing home. Moving speculatively violates premature-abstraction rule. Trigger: when the next form-composing view is built, move all 6 to core.css, remove from mcc.css, and re-verify mcc.css size drops back below 300 soft cap. Reduces 486-line leaf substantially | open | 0d |
| 2026-05-02 | docs/marketing/NOTION-SETUP.md shipped (187 lines) — Dan to actually create the MCC Posts DB and run §7 checklist | New authoritative DB-setup doc covering 11-property MCC Posts schema (cross-linked to drafter.js + read-model.js line numbers), one env var, notion-hub.md update step, Phase 2 stubs honestly flagged as "no code yet". Supersedes SETUP.md §1-2. Phase 1 setup ~15 min. After Dan ships the DB and confirms `curl /api/mcc/status` returns six zero counts, Phase 1 is fully wired end-to-end | open | 0d |
| 2026-05-02 | docs/marketing/ MCC docs — review surfaced 1 bug + 5 inconsistencies + 3 minor nits, awaiting Dan's remediation choice | Reviewed all 5 files (README/SETUP/ARCHITECTURE/API/USER_GUIDE) against actual code. Bug: SETUP.md:46-55 instructs hardcoding DB ID at read-model.js line 8 but the constant is at line 13 and reads from env (process.env.MCC_POSTS_DB_ID) — step 2 should be deleted. Inconsistencies: README ✅+(stub) for OAuth, API.md POST /posts approval-gated claim needs verification against routes/mcc.js, ARCHITECTURE "read-model pattern" terminology collides with Phase 1 cross-domain read-model layer, SETUP notion-hub.md two-step trap, USER_GUIDE OAuth flow missing prereq pointer. Minor: ❌ marks read as rejected, state machine missing terminal/retry edges, API errors lack HTTP status codes. Pending Dan: (a) commit as-is + patch later, (b) self-fix in single Edit pass, (c) dispatch scribe with diff list | open | 0d |
| 2026-05-02 | daily-sales.css split — DONE (1 shim + 10 leaves + FILE-MAP.md) | Shipped 2026-05-02 05:30. 2836 → 18-line shim. 10 leaves all under 600 cap (largest: lens 493). Build clean (esbuild @import inlining works), 1014/1014 tests, lint clean, bundle 42089→40367 bytes (minor minification gain). 4 misplacement clusters caught and fixed in a 2nd pass + manual finishing. FILE-MAP.md documents 2 cross-family exceptions (status-* color tokens in base.css alongside .ds-status chrome; compound .ds-channel-pill + .ds-tag-method rule kept intact in mtd-channel.css). Decision #90 logged. Dan hard-refresh of /daily-sales pending. Unblocks the daily-sales.html inline-style migration (session 2) | done | 0d |
| 2026-05-02 | daily-sales.html inline-style migration (22 instances ≥30 chars) — UNBLOCKED, ready for session 2 | Was blocked behind the daily-sales.css split (now done 05:30). Each leaf has plenty of room (largest is lens at 493/600). Inventory the 22 instances by panel — each will land in its panel's leaf (e.g. orders inline styles → todays-orders.css, lens KPI inline styles → lens.css). Standard mechanical extraction pattern, same as marketingOps and ops sessions | open | 0d |
| 2026-05-02 | marketingOps.css at 600-line hard cap — next addition requires split first | 28 inline-style instances migrated to 7 `mktops-*` classes added at lines 560-600. File now exactly at the file-discipline.md hard cap. Apply notion/sheets/daily-sales split methodology (Decisions #72/#79/#80) when next CSS change to marketingOps lands. Suggested split axes: filter/header section, BMC/canvas section, skeleton/empty-state utilities, mktops migration block | open | 0d |
| 2026-05-02 | marketingOps.html inline-style bloat — DONE (28→0 repeated patterns; 24 one-offs left inline) | Shipped 2026-05-02 04:35. 7 classes added (mktops-empty-spaced/bmc-block-body/ai-input/ai-input-last/skeleton-col/select-pointer/card-pointer). Build + 1014 tests + Alpine directive integrity all green. Dan hard-refresh of /marketing-ops still pending | done | 0d |
| 2026-05-02 | Inline-style bloat — remaining partials from 03:35 punch list | DONE for dashboard/techTeam/overview (06:15 — 26 inline styles → 8 classes; counts now dashboard 38, techTeam 11, overview 5). Still open: daily-sales 22 (now unblocked after split). ops.html DONE (Set 1 04:10 + Set 2 05:10). marketingOps DONE (04:35). Pre-existing one-offs left across all done partials (skeleton heights used once, dynamic widths, single-use layouts) — by design, not a regression | open | 0d |
| 2026-05-02 | dashboard/techTeam/overview inline-style migration — DONE (8 classes, 26 styles replaced, new overview.css) | Shipped 06:15. dashboard.css 676→691 (+3 classes), techTeam.css 414→425 (+2), new overview.css 24 lines (3 classes), one-line app.js registration at line 119. Build clean, 1014/1014 tests, lint clean. Inline counts: dashboard 47→38, techTeam 17→11, overview 16→5. Trimmed 3 dashboard spacing utilities from initial proposal as zero-gain noise relocation. Dan hard-refresh of all 3 views still pending; code-reviewer dispatch queued post-confirm | done | 0d |
| 2026-05-02 | dashboard.css split (676→leaves) deferred tech debt | Skipped during 06:15 inline-style migration per Dan's call (option (a)) — adding 3 minimal classes didn't justify full split package. Bundle with google-ads.css split (also over cap at 672) when next view-CSS work lands. Apply notion/sheets/daily-sales split methodology (Decisions #72/#79/#80/#90) | open | 0d |
| 2026-05-02 | ops.html inline-style bloat — DONE (Set 2: 11 classes, 82 applications; 6 left = 2 dynamic + 4 one-offs) | Shipped 2026-05-02 05:10. ops.css 156→210 lines (well under 500 cap). 11 classes: ops-mb-125/-10/-15, ops-card-flush, ops-card-header, ops-scroll-x, ops-table-sm, ops-empty-cell, ops-card-label-block, ops-cell-truncate-160 (defensively defined, unused), ops-mono-sm. Build + 1014 tests + Alpine integrity green. code-reviewer APPROVED. Dan hard-refresh of /ops (5 tabs) still pending | done | 0d |
| 2026-05-02 | Commit + push dashboard cockpit metric cards layout fix | `.mktops-area-metrics` grid rule moved from `views/marketingOps.css` to [src/css/core.css:10542-10546](src/css/core.css#L10542-L10546) so dashboard view (which doesn't load marketingOps.css) gets the 4-col grid. `npm run build` clean, built core.css verified. Bundle with the other 2 uncommitted CSS fixes for single push/deploy. Dan to hard-refresh `/dashboard` first to confirm cards sit side-by-side | open | 0d |
| 2026-05-02 | App-wide structural audit punch list — Dan to triage | Bundle-leakage scan came back clean (1924 classes / 16 views, only the cockpit leak was real). UX pass surfaced: (a) 5 partials diverge from canonical `view-header-bar` pattern (chat/dan-colin/focusArea/google-ads/personView — chat probably correct to differ); (b) inline-style bloat in 6 partials (marketingOps 28, ops 26, daily-sales 22, techTeam 16, dashboard 16, overview 13); dominant repeated pattern is text-truncation snippet (~6× in ops.html) wanting a `.cell-truncate` utility, plus `display:flex;gap:4px;align-items:center` (~8×) wanting `.flex-row-tight`. Two paths offered: polish-only (utilities + ~30 migrations, ~20 min) OR full pass (also align 5 divergent headers, ~1.5h). Awaiting Dan's pick before frontend-builder dispatch | open | 0d |
| 2026-05-02 | Commit + push `/api/*` JSON 404 handler | Fix landed locally at [server.js:177-183](server.js#L177-L183), lint + 1014/1014 tests pass, curl-verified. Working tree dirty. Next: commit, push, re-verify prod with `curl -i https://yds-command-centre.vercel.app/api/dashboard` (expect 404 JSON, not 200 HTML). Decision #89 logged | open | 0d |
| 2026-05-02 | Commit + push sidebar nav scrollbar fix | `.sidebar` got `overflow-y: auto` + `overflow-x: hidden` + `overscroll-behavior: contain` at [src/css/core.css:314-329](src/css/core.css#L314-L329). `npm run build` ran clean, built `public/css/core.css` contains props. Working tree dirty. Pair with `/api/*` 404 fix in same commit. Dan to hard-refresh + confirm scrolling works on short viewport / all-groups-expanded before push | open | 0d |
| 2026-05-02 | Google Ads — repoint parseCrunchingRows from Data_Crunching to Raw_Data | Path (a) header fix landed for `Raw_Data` (byDevice now correct). `Data_Crunching` left untouched — it's structurally broken (no clicks/conversions/revenue cols at source; col 6 "Real Cost" is ad_group_id formatted as ₹; col 8 "Revenue" is #VALUE!). Fix needs backend-builder pass: drop `GOOGLE_ADS_CRUNCHING` fetch in google-ads-service.js, build campaign rollup from `Raw_Data` (groupBy campaign.name; sum cost_micros/clicks/conversions/conversions_value). ~30 lines. Will fix KPI totals + campaign table + daily trend | open | 0d |
| 2026-05-02 | Google Ads sheet header rewrite — Raw_Data | Wrote 23-col corrected header via service-account RW client (`Raw_Data!A1:W1`). byDevice section verified correct. Old header preserved in handoff transcript if revert needed | done | 0d |
| 2026-05-02 | Google Ads page — untapped source tabs | Spreadsheet has 7 unused tabs: `Raw_Conversions` (real conv data — page currently shows 0), `Funnel_Data` (currently hardcoded in service), `Budget_Pacing`/`Budget_Targets`, `Raw_Keywords` (56k rows), `Raw_Search_Terms_Waste`. Light up after path-a header fix lands | open | 0d |
| 2026-04-08 | App Split & Optimization | Phase 1 shipped; Phase 2 complete; Phase 3 fully complete (Step 2 Batches A–E + Legs 1–3 cleanup + Step 3 styles.css retirement) | Done — shipped 2026-05-02 01:15. styles.css deleted (10,161 lines), leakage 0, all gates green | 0d |
| 2026-05-01 | Phase 3 CSS Batch E — Dan browser smoke | All 200 single-partial styles.css orphans drained across 8 destination view files (registry/ops/marketingOps/knowledge/crm/claude-usage/bmc/techTeam). Build clean, 1014/1014 tests, lint clean. Hard-refresh each view and confirm no visual regression before Step 3. | open | 0d |
| 2026-05-01 | Phase 3 Step 3 — retire src/css/styles.css | Drop `<link>` + entryPoint + delete src + delete built artifact | Done — shipped 2026-05-02 01:15. 4 mechanical actions, 6 validation gates ✅. styles.css 10,161 lines deleted. Decision #82 (safety-net loading) effectively superseded | 0d |
| 2026-05-02 | Update .claude/rules/frontend-patterns.md to drop styles.css legacy references | Step 4 followup — reflect post-Phase-3 state (core.css + views/<view>.css only, no legacy monolith). Owner: scribe | open | 0d |
| 2026-05-01 | check-css-leakage.mjs orphan-count fix | Add total orphan-row count to summary block at top — was root cause of 22:25 audit miscount when awk range collapsed on the trailing `## styles.css orphans` heading. Single-line addition. Owner: devops-infra | Done — shipped 2026-05-01 23:25 as part of Leg 2 (folded into Alpine state whitelist work in scripts/check-css-leakage.mjs) | 0d |
| 2026-05-01 | Phase 3 CSS Leg 3 — triage 82 remaining undefined classes | Legs 1+2 shipped (104→82 rows). | Done — shipped 2026-05-02 across 4 batches (orchestrator pre-write methodology, no agent stream-caps). Final 81→12 (12 deferred behind file-discipline splits — see new row below). Decisions #85 (pre-write methodology) + #86 (empty marker rules) logged | 0d |
| 2026-05-01 | Phase 3 CSS Legs 1+2+3 — Dan browser smoke | All 3 legs shipped (104→12 leakage rows). Affected: `/dashboard` (brief-banner section + bulk-action btn-done/snooze), `/daily-sales` (drill-sheet open animation, ds-geo-row), `/system-map` (view status cards), `/marketing-ops` (UTM Builder — first time it has CSS), `/docs` (doc-name), `/overview` `/status` (org-last-refresh, attention-upcoming counter), `/techTeam`. Mechanical adds — no visual regression expected | open | 0d |
| 2026-05-02 | Phase 3 CSS — 12 deferred classes blocked on view-CSS splits | All deferred behind file-discipline hard-cap rule. Each cluster needs its destination view-CSS split first. (1) `views/google-ads.css` (672 lines) → unblocks `.ga-error/-unavailable/-tab-content/-status-table/-campaign-table/-search-table` (6 classes). (2) `views/dashboard.css` (676) → unblocks `.pipeline-placeholder` (1). (3) `views/daily-sales.css` (2836 — biggest god-CSS in repo) → unblocks `.ds-weekly-chart` (1). (4) `views/marketingOps.css` (565) → unblocks `.filter-pill-count`, `.kanban-card-title` (2). (5) `views/techTeam.css` (414, soft cap only) → unblocks `.tech-crm-breaches`, `.tech-strategy-level` (2). Apply notion/sheets/daily-sales split methodology (Decisions #72/#79/#80) — flat package + FILE-MAP.md per CSS view bundle | Superseded by Batch 5 proposal (next row) — Dan asked whether the 12 actually need fixing; bypass via core.css markers proposed | 0d |
| 2026-05-02 | Phase 3 CSS Batch 5 — drain last 12 leakage rows via core.css markers (BYPASS view-CSS splits) | Dan green-lit, batch shipped. core.css +87 lines (11096→11183), 6 wrapper markers + 6 real minimal rules. Leakage 12→0. All gates pass. Decision #88 logged | Done — shipped 2026-05-02 01:00 | 0d |
| 2026-05-02 | Phase 3 CSS — leakage at 0, 5 view-CSS splits queued as regular tech-debt | Not blocking anything (markers in core.css cover the gap; lint is clean). Splits become normal cadence work: google-ads.css 672, dashboard.css 676, daily-sales.css 2836, marketingOps.css 565, techTeam.css 414. Apply notion/sheets/daily-sales split methodology (Decisions #72/#79/#80) when each comes up. After each split lands, migrate the matching marker rules from core.css to their proper view file (decision #88 noted this migration path) | open | 0d |
| 2026-04-08 | x-show→x-if verification | 14 views converted, reviewers never returned results | Pending | 0d |
| 2026-04-08 | Activity log cleanup | ~150 stale `file-write \| unknown` entries from Mar 26–31 | Pending | 13d |
| 2026-04-30 | Daily Sales Master Spec implementation choice | Dan picked (b). Pass 1 + Pass 2 backend shipped (215 tests pass, code-reviewer PASS WITH NOTES on both); View A frontend shipped (filter strip rework, MTD by Channel, weekly trend, concerns split, DQ additions). Awaiting Dan browser test before Pass 3 (View B + Account Tier backend) | Done | 0d |
| 2026-04-30 | docs/daily-sales-spec.md vs Master Spec reconciliation | Still pending — spec at 15:08 not yet repointed. Master Spec is canonical | Pending Dan | 0d |
| 2026-04-30 | Daily Sales View A — Dan browser test | Frontend shipped end of session. Dan to test 4-row filter strip, MTD by Channel click-to-filter, weekly trend, mix label translations, Today's Orders pills/badges, Concerns 3-section split, DQ additions, 390px mobile. After sign-off: code-reviewer + ux-auditor on frontend then Pass 3 | Pending Dan | 0d |
| 2026-04-30 | aggregations.js over hard cap (439 lines, cap 400) | Next backend change to that file MUST be preceded by a split into `aggregations/{revenue,mix,trend,concerns,today,topstates}.js` per Decision #75. Split proposal documented in FILE-MAP.md. Blocking for any aggregations.js edit, not for frontend | Pending | 0d |
| 2026-04-30 | daily-sales.html partial over hard cap (2,183 lines, cap 500) | View A additions pushed it from 1,699 → 2,183. Split proposal: `daily-sales/{filter-strip,hero-strips,mtd-by-channel,trends,mix,top-states,todays-orders,concerns,data-quality}.html` via server-side compose. Separate refactor ship. Not blocking | Pending | 0d |
| 2026-04-30 | Account Tier mix card | Placeholder hidden (`x-show="false"`) in mix grid. Backend needs to expose `mix.accountTier[]` aggregated server-side from Partner orders. Pass 3 work alongside `salesDesk` block | Pending | 0d |
| 2026-04-30 | Mix row click behavior decision | Currently opens drill-sheet (richer UX, was already implemented). UI Spec §9 says it should apply the corresponding filter chip instead. Keeping drill-sheet pending Dan's confirmation | Pending Dan | 0d |
| 2026-04-30 | 30-day trend area-fill bug (pre-existing) | `_dsComputeTrendPaths()` writes `primaryFill`/`secondaryFill`; SVG template reads `revenueFill`/`ordersFill`. Area fills under the lines never render. Not introduced this session; surfaced during View A work. Single-line fix in either the JS or the template | Pending | 0d |
| 2026-04-30 | Daily Sales Pass 3 — View B (Sales Desk) + mix.accountTier | Backend: `salesDesk` block when `?madeBy=` is set (rep MTD numbers, pipeline, topCustomers, orderTypeMix); `mix.accountTier[]` server-side aggregation. Frontend: new view at `/dashboard/sales` with rep dropdown, pipeline table, top customers table, order mix card. Per UI Spec §6 + §10. Starts after View A signs off | Queued | 0d |
| 2026-04-30 | Channel group pill colors | D2C/Corporate/DS/Stores pills use existing token approximations (blue/purple/teal/orange). May want a design pass with `design-planner` if Dan's picky about hue precision | Pending | 0d |
| 2026-05-01 | Daily Sales v5 — Dan browser test | Lens segment-control (Realized/Active/All + Custom), stat-card tooltips (Yesterday/MTD/YTD info icons), freshness banner states (error/stale/normal) shipped. Backend split (637→75 line index.js + 4 siblings + FILE-MAP.md) shipped. Awaiting Dan to load `/daily-sales` and verify lens labels, lens math (Active should ≈ 1179 = 966 realized + 213 in-flight), tooltip clipping, hash bookmark persistence | Pending Dan | 0d |
| 2026-05-01 | docs/daily-sales-spec.md | Starter prompt drafted for fresh Claude Code session. Dan to spawn the session; output is a complete shareable schema/pipeline/calc doc agents write by reading the code (no memory-guess) | Pending Dan | 0d |
| 2026-05-01 | Daily Sales reconfigure — data team alignment | 7-question discovery prompt drafted (order types, status buckets, channels, payment modes, tags, missing fields, time windows). Dan to convene team + create Notion SSOT page "Daily Sales — Reconfigure" with one row per question, name a data-hygiene owner | Pending Dan | 0d |
| 2026-05-01 | Daily Sales reconfigure — tech track parallel chat | Kickoff prompt drafted for parallel Claude Code session in this repo. Sits idle until Dan pastes decided Notion rows; implements per-row + updates FILE-MAP.md as routing changes | Pending Dan | 0d |
| 2026-05-01 | Frontend mirror-split (daily-sales) | After v5 browser sign-off: split `src/js/modules/daily-sales.js` into `daily-sales/{state,lens,tooltip,banner,filters,index}.js` + write a frontend FILE-MAP.md mirroring backend pattern. Plus optional partial split. Same agent pipeline (frontend-builder → code-reviewer) | Queued | 0d |
| 2026-05-01 | Scribe pass for v3+v4+v5+refactor | After v5 browser sign-off: scribe updates `.claude/docs/app-reference.md` with new classes (lens, tooltip, banner-state modifiers), API fields (`filters.appliedStatusList`, `filters.applied.excludeStatuses`, `Order.statusBucket`), helpers (`_lensToFilter`, `applyLensFilter`, `_detectLens`, `toggleDsTooltip`, `dsTooltipData`, `_computeStaleDays`, `_isSheetsError`, `formatDataCutoff`, `formatCutoffShort`, `formatRelativeTime`), and FILE-MAP.md reference. Also pending Dan↔Colin DB-ID fixes from prior session | Queued | 0d |
| 2026-05-01 | server/services/notion.js god-file split | ~1000 lines, biggest remaining god-file in app, used by every domain service. Apply the daily-sales split pattern (public-API shim + per-concern siblings + FILE-MAP.md + lazy-require contract) once frontend daily-sales split confirms the methodology travels well. Backend-builder + code-reviewer pipeline | In progress — PR1 shipped 2026-05-01 (9 infra leaves, 2721→2049 lines, suite green). PR2 (domain reads/writes) and PR3 (composers + shim + FILE-MAP) queued | 0d |
| 2026-05-01 | notion.js split plan — Dan approval | Plan handed to Dan: flat package, 24 files, largest ~280 lines, dep graph acyclic, public API (57 exports) frozen, 3-PR phasing. Pre-PR1 verification needed: confirm lazy-require contract by reading test/notion-service.test.js. On approval becomes Decision #79 | Done — Dan approved, PR1 shipped 2026-05-01. Lazy-require NOT needed (test mocking pattern doesn't use require.cache injection) | 0d |
| 2026-05-01 | notion.js split PR2 — domain reads + writes | After PR1 ships: extract reads (focus-areas, people, projects, decisions, commitments, marketing-ops, tech-team, ai-team, tasks) and writes (commitments, marketing-ops, tech-team) into `notion/reads/*.js` and `notion/writes/*.js`. notion.js shrinks to ~600 lines (dashboard summary + morning brief + exports). Same pipeline: backend-builder → code-reviewer → tester | Done — shipped 2026-05-01. notion.js 2049→602 lines, 12 new files, lint clean, 1014/1014 pass, 56 exports preserved | 0d |
| 2026-05-01 | projection-job-store test flake | Test had afterEach cleanup but no beforeEach — running full suite twice (or interleaved with read-model-scheduler/health-route tests that touch projection-jobs.json) left residue that broke the first assertion. 4-line `beforeEach(rm)` fix | Done — verified 1014/1014 across 2 consecutive runs | 0d |
| 2026-05-01 | Commit PR1 + PR2 + flake fix | All three changes are uncommitted. Suggested as 3 separate commits (test fix, PR1 mechanical refactor, PR2 mechanical refactor) for clean review history | Pending Dan | 0d |
| 2026-05-01 | notion.js split PR3 — composers + entry shim + FILE-MAP | After PR2: move getDashboardSummary + buildMorningBrief into `notion/dashboard-summary.js` + `notion/morning-brief.js`, route raw `cache.get(DASHBOARD_CACHE_KEY)` through `getCachedWithTTL` helper (already added to cache.js in PR1), add `notion/index.js` (re-exports), reduce notion.js to 3-line shim, ship FILE-MAP.md. Final state per the approved plan | Done — shipped 2026-05-01. notion.js 602→4 lines. 3 new files (dashboard-summary 293, morning-brief 202, index 171). FILE-MAP 253 lines. `getPage` in index.js (not pages.js) to avoid cycle. lint clean, 1014/1014 pass | 0d |
| 2026-05-01 | PR1 commit | 9 new files + modified notion.js, lint clean, 1013/1014 tests pass (1 pre-existing flake unrelated). Awaiting Dan's go-ahead on commit message | Superseded — bundled with full split commits below | 0d |
| 2026-05-01 | server/services/notion.js god-file split | ~1000 lines, biggest remaining god-file. PR1 (infra leaves) + PR2 (domain reads/writes) + PR3 (composers + shim + FILE-MAP) all shipped this session | Done — 2721→4 lines, 24-file package + FILE-MAP, 56 exports preserved, 1014/1014 tests | 0d |
| 2026-05-01 | Commit notion.js split (4 commits) | Test flake fix + PR1 + PR2 + PR3 all uncommitted. 4 sequential commits ready | Done — shipped 2026-05-02 02:00 as 2 commits: `5f48e60` (test flake) + `1a94659` (notion split — single bundled commit covering PR1+PR2+PR3 since they were already shipped as code) | 0d |
| 2026-05-01 | God-file candidates surveyed for next splits | Server: ceo-dashboard-service.js (1049), **sheets.js (759 — highest leverage, used by every Sheets-backed service)**, overview-service.js (751), system-map-service.js (406), read-model-store.js (402). Frontend: app.js (1879 — held per #78), daily-sales.js (1240, queued), marketing-ops.js (1085), factory.js (913). Partials: daily-sales.html (queued), marketingOps.html (1761). CSS globals (styles.css, core.css) flagged as different methodology. Recommended next 3: sheets.js → ceo-dashboard-service.js → marketing-ops.js | Survey done — Dan to pick which to tackle next | 0d |
| 2026-05-01 | Next-session prompt ready (sheets.js split) | Self-contained paste-and-go prompt drafted for fresh Claude Code session targeting `server/services/sheets.js` (759 lines, highest ROI). Includes pre-flight checklist, methodology references (#73 lazy-require, #74 FILE-MAP, #75 file-discipline, #79 notion methodology), validation gates, recommended approach. Heads-up included that notion split commits are still uncommitted | Done — split shipped 2026-05-01 (single PR, 759→19 shim + 9 siblings + FILE-MAP, 1014/1014 pass on 2 consecutive runs, decisions #80 + #81 logged) | 0d |
| 2026-05-01 | sheets.js god-file split | 759 lines → 9-file flat package + FILE-MAP.md + 19-line eviction-loop shim. 16 public exports preserved, dep graph acyclic, largest sibling 195 lines. Decisions #80 (split methodology) + #81 (eviction-loop shim pattern — required for packages whose tests inject mocks via `require.cache[ENTRY_PATH]`; distinct from notion's pattern per #79) logged | Done — 1014/1014 tests pass on 2 consecutive runs, lint clean, code-reviewer APPROVE, scribe updated app-reference.md | 0d |
| 2026-05-01 | Commit sheets.js split | Single commit ready | Done — shipped 2026-05-02 02:00 as `5b57d04` | 0d |
| 2026-05-01 | BMC area-dashboard panel unstyled — diagnosed | Root cause: `mktops-overview-shell` / `mktops-overview-grid` / `mktops-priority-card` (+ children + tone modifiers) defined ONLY in [src/css/views/marketingOps.css](../../src/css/views/marketingOps.css), but used by 7 partials. Each view lazy-loads only its own `/css/views/<view>.css`, so non-marketingOps views render the priority-card grid unstyled. Unrelated to sheets.js split — pre-existing Phase 3 view-CSS-split issue | Done — Path A shipped 2026-05-01: 26 shared selectors moved to core.css, 1 combined selector split, build clean. Awaiting Dan browser test for visual confirmation | 0d |
| 2026-05-01 | BMC + marketing-ops browser smoke test | Frontend-builder moved 26 mktops/hero/priority selectors from marketingOps.css → core.css (+~155 lines). Hard-refresh /bmc to confirm area-dashboard hero + 3-card priority grid render correctly. Also /marketing-ops to confirm no regression | Pending Dan | 0d |
| 2026-05-01 | 7-view CSS leakage audit — round 2 | Static analysis identified additional shared classes still scoped to marketingOps.css after the 26-selector move. Migration list ready: `.mktops-view` (wrapper, used by all 7), `.mktops-area-metrics` + 4 `.mktops-area-metric-{card,label,value,note}` (used by crm/dashboard/factory/ops/techTeam), `.mktops-tabs` + `.mktops-filters` base (crm/ops), `.mktops-alert` + `.mktops-alert-danger` (techTeam). Browser verification confirmed bmc OK, crm/dashboard broken as predicted, plus surfaced `brief-banner-*` (factory.css→dashboard) and owner-workload action buttons. Re-diagnosed as styles.css orphan (Phase 3 incomplete) | Done — superseded by safety-net fix. Decision #82 ships styles.css load alongside core.css | 0d |
| 2026-05-01 | Phase 3 CSS safety net — Dan browser verify | `public/index.html` now loads `/css/styles.css` after `/css/core.css`. Build script extended to emit it (218KB minified). Hard-refresh /crm /dashboard /factory /ops /techTeam to confirm area-metric strips, brief-banner panels, owner-workload action buttons all render correctly | Done — Dan confirmed dashboard rendering improved; superseded by tone-coded redesign | 0d |
| 2026-05-01 | Dashboard area-metric tone-coded cards — Dan browser verify | Tone-coded `.mktops-area-metric-card` shipped (Decision #83). Backend computes `tone` per metric, frontend applies `--{tone}` class + icon + chevron + hover lift. Hard-refresh /dashboard — verify Open Commitments / Action Queue / Active Projects render `critical` (red border, red value, red note pill); Recent Decisions stays `neutral`; icons top-left; chevron on hover | Pending Dan | 0d |
| 2026-05-01 | Extend tone-coded cards to crm/factory/ops/techTeam | Same shared class — backend pattern (computing `tone` per metric in each view's `getXHeroMetrics()` function) needs replicating per view. Hold until Dan confirms Option A direction on dashboard | Queued | 0d |
| 2026-05-01 | Tone-coded cards — Option B sparklines follow-up | If A validates: add 30-day trend SVG per metric (right side of card) + delta arrow vs prior period. Backend exposes trend arrays per metric. Hand-rolled SVG per Decision #67 | Queued | 0d |
| 2026-05-01 | Tone-matched hover borders | Frontend-builder swapped spec's `color-mix(currentColor)` hover rule for box-shadow + translateY because currentColor at card level resolves to text color, not the tone accent. Per-tone hover rule (`.card--critical:hover { border-color: var(--red) }` etc.) is the correct fix — small follow-up | Queued | 0d |
| 2026-05-01 | code-reviewer + ux-auditor + scribe pass for tone-coded cards | Held until Dan signs off browser test (per stored feedback: pause after frontend-builder on UI changes). On approval, dispatch all three. Scribe to update `.claude/docs/app-reference.md` with new tone classes + `getDashboardMetricIcon` helper | Queued | 0d |
| 2026-05-01 | Filters audit batch 1 — CSS migration | Move 4 leaking classes from styles.css/views to core.css: `.saved-view-pills` (6 views), `.crm-tab-count` (crm+ops), `.table-inline-actions` (3 views), `.filter-select` (ops). Same methodology as round-1 hero/priority work. ~15 min via frontend-builder | Done — shipped 2026-05-01 16:50, build clean, duplicates removed from styles.css + view files | 0d |
| 2026-05-01 | Filters audit batch 2 — "Clear filters" buttons | 9 buttons added across 7 partials: bmc:172, crm:292 (leads) + :361 (flows), ops:420 (stock) + :660 (products), decisions:42, projects:23, registry:45, techTeam:336. Template at `commitments.html:45`. factory.html SKIPPED — pills are preset launchers, no filter state object | Done — shipped 2026-05-01 16:50, lint + build clean. Pending Dan visual verification | 0d |
| 2026-05-01 | Filters audit batches 1+2 — Dan browser verify | Hard-refresh and click Clear on bmc / crm (both tabs) / ops (both tabs) / decisions / projects / registry / techTeam. Spot-check saved-view pills, crm-tab-count badges, table-inline-actions still render on dashboard/factory/marketingOps/techTeam (Batch 1 moved CSS) | Pending Dan | 0d |
| 2026-05-01 | factory.html Clear filters — needs state-object refactor | Filter pills on factory are `applyFactoryQuickPreset` launchers, not state bindings. To add a Clear button, factory's filter UX would need a refactor to a true filter-state object. Separate batch, low priority | Queued | 0d |
| 2026-05-01 | Filters audit batch 3 — class consolidation (deferred) | Unify `filter-search` / `filter-select` / `calendar-filter-select` for selects; unify pill class family (`filter-pill` / `ds-lens-pill` / `crm-status-pill` / `saved-view-pills`). Design-system work — needs design-planner pass first. Separate session | Queued | 0d |
| 2026-05-01 | CSS-leakage lint script | Build `scripts/check-css-leakage.mjs`: for each partial in `public/partials/*.html`, extract `class="…"` tokens, find which CSS files define each, fail if a class is used by a partial but defined only in a CSS file that partial doesn't load. Output orphan list = migration backlog | Done — fix-up shipped via agent `a93ef4cf6876b53ca`. 179 lines (under cap), regex catches compound selectors, "## styles.css orphans" section present (401 rows), sanity check passes. Lint exits 1 as expected. Output at `/tmp/css-leakage-v2.txt` | 0d |
| 2026-05-01 | Phase 3 Step 2 — frontend-builder migration | Bulk orphans are `factory-*` family (→ views/factory.css) and `dashboard-*` / `health-*` / `brief-banner-*` family (need new views/dashboard.css). Plus 204 cross-partial leaks (views/marketingOps.css → crm/ops/factory/dashboard/techTeam, views/factory.css → dashboard, views/bmc.css → crm, views/registry.css → knowledge/system-map). Awaiting Dan's authorization on scope before frontend-builder dispatch | Pending Dan | 0d |
| 2026-05-01 | build-assets.mjs styles.css entryPoint addition | Both lint script passes left a `+ path.join(cssDir, 'styles.css')` change in `scripts/build-assets.mjs` that's outside the brief's scope. Likely fixes a Decision #82 safety-net 404 (styles.css link in index.html had no built file). Decide whether to commit with Step 1 lint or with Step 3 retirement | Pending Dan | 0d |
| 2026-05-01 | Phase 3 CSS migration pass | After lint runs: move orphan classes out of styles.css into core.css (shared) or views/<view>.css (single-view per partial usage). Remove the safety-net `<link>` from index.html and the `styles.css` entry from `build-assets.mjs`. Verify lint green. Retires styles.css as a source-of-truth | Queued | 0d |
| 2026-05-01 | frontend-patterns.md rule update | After migration: codify "shared classes live in core.css, view-specific classes live in views/<view>.css, lint enforces this." Decision row alongside | Queued | 0d |
| 2026-05-01 | Code-reviewer + ux-auditor + scribe pass for CSS fix | Held until Dan signs off browser test (per stored feedback: pause after frontend-builder on UI changes). On approval, dispatch all three to validate the move + update app-reference.md if needed | Queued | 0d |
| 2026-05-01 | Future: rename `mktops-*` to prefix-neutral | The `mktops-` prefix is misleading — these classes are shared infra used by 7+ views (bmc, crm, dashboard, factory, marketingOps, ops, techTeam). Path B from this session's diagnosis. Bigger touch (updates 7 partials + CSS file), no functional benefit beyond clarity. Revisit when CSS architecture work surfaces | Pending — not blocking | 0d |
| 2026-05-01 | 3 test fixes pending Dan's go-ahead | Verified diagnoses on dan-colin-service.test.js (4× `afterEach` ReferenceError, fix import), auth-gate.test.js (test process hangs — fix is `.unref()` on `setInterval` at server/middleware/auth-gate.js:160), notebooks.test.js:171 (cache-miss reference equality — pick service-side cache or test-side fixture). User-supplied fixes were partly wrong; corrected versions ready to apply | Done — all 3 shipped 2026-05-01 02:08, full suite 1014/1014 pass, exit 0 | 0d |
| 2026-05-01 | App split spec cleanup decision | `docs/app-split-config-spec.md` reviewed — factual errors (29 vs 39 modules, 9,988 vs 11,867 lines), typo plague, scope creep (5 concerns bundled). Recommended 3-way split into separate proposals + drop meta.json/TypeScript/e2e from migration | Done — Shelved per decision #78 (revisit when notion.js split lands or app.js crosses 2500 lines) | 0d |
| 2026-05-01 | Daily Sales 30-day trend area-fill bug | Bug verified: template reads `revenueFill`/`ordersFill`, JS writes `primaryFill`/`secondaryFill` | Done — shipped 2026-05-02 02:00 inline as part of `2dd50ce`. Renamed JS to match template (revenueFill/ordersFill). Build + tests pass | 0d |
| 2026-05-01 | Commit + push 3 test fixes | All 3 fixes verified, full suite green | Done — pushed as `7dc66e3` (`584441e..7dc66e3`), Vercel auto-deploying | 0d |
| 2026-05-01 | Working tree triage | ~30+ files modified across .claude/agents/*, .opencode/* (deletion), AGENTS.md, CLAUDE.md, server/routes/bmc.js, daily-sales V5 work, etc. Accumulated from prior sessions, never committed. Needs Dan to triage into separate logical commits | Done — shipped 2026-05-02 02:00 as 13 logical commits (`5f48e60`..`a6942b2`). All gates green. Push pending Dan green-light | 0d |
| 2026-05-01 | Phase 3 CSS Step 2 Batch B — dashboard family migration | frontend-builder agent `a5f540a26a1fa5ef8` shipped: created `src/css/views/dashboard.css` (676 lines, 93 selectors, 35 class families: dashboard-/health-/brief-banner-/activity-item-/overdue-group-/workload-/team-workload-strip), wired `dashboard: 'dashboard'` in `src/js/app.js` `_viewStyleFile` map, factory.css 1826→1186 (−640), styles.css ~13727→13088 (−639). Build clean (16 lazy bundles, was 15), 1014/1014 tests, lint clean, leakage check confirms migration | Done — pending Dan visual smoke `/dashboard` + `/factory` | 0d |
| 2026-05-01 | Batch B browser smoke test | Hard-refresh `/dashboard` (morning-brief banner, health-strip rows, overdue-triage zone, activity-feed list, workload chips) and `/factory` (confirm nothing accidentally moved out — factory.css now 640 lines smaller). NO style regressions vs current state | Pending Dan | 0d |
| 2026-05-01 | views/dashboard.css over 500-line soft cap (676 lines) | Holds two distinct concerns: (a) legacy `morning-brief`/`brief-*` widget family, (b) newer `dashboard-*` 3-zone layout + health lanes + activity/overdue feed. Builder did not split per "no splits during this batch" constraint. Future split candidates: `views/dashboard/{brief-banner,zones-and-health,activity-feed}.css` | Queued | 0d |
| 2026-05-01 | Phase 3 CSS Step 2 — Batch A (factory-* family) | Largest styles.css drain. Migrate factory-* family from styles.css → views/factory.css. Use same one-batch-at-a-time pattern as Batch B brief. Routing-rule check: any class used by 2+ partials goes to core.css instead | Done — shipped 2026-05-01 20:50 via frontend-builder `ad45cb3939071ea4b` (187s). Mostly delete-only on styles.css duplicates (rules already in views/factory.css). styles.css: 13803→11857 (−1946 cumulative incl Batch B). views/factory.css: 1826→1226. Residual `\.factory-` selectors in styles.css = 0. Build + 1014/1014 tests + lint clean. 5 residual leakage rows on factory partial are all multi-partial shared classes correctly skipped for Batch D | 0d |
| 2026-05-01 | Batch A browser smoke test | Hard-refresh `/factory` — verify zone bars + meta render, machine table, cascade items, shared rows, sim panel + presets (open simulator), assumptions read/edit toggle, KPI summary row, factory icon + role badges. NO style regressions vs current state | Pending Dan | 0d |
| 2026-05-01 | Phase 3 CSS Step 2 — Batch C remainder (mktops cleanup) | After agent `a3b483a4dd26063b9` partial run: `mktops-tabs`, `mktops-alert*`, `view-refresh-label`, `area-card-button`, `compact-empty-state`, `data-card-button`, `tech-system-tag-btn`, `action-btn-danger` still leaking. Multi-partial → core.css | Done — shipped 2026-05-01 21:25 via frontend-builder `ae3219270c376fe39` (372s). core.css +83 (10899→10982), marketingOps.css −310 (~644→334, mostly delete-only of duplicates already in core.css), styles.css −74 (11857→11783), techTeam.css −10. Leakage 341→311 (−30). mktops-alert* correctly stayed (single-partial). Build + 1014/1014 + lint clean | 0d |
| 2026-05-01 | Batch C browser smoke test | Hard-refresh `/marketing-ops`, `/crm`, `/dashboard`, `/factory`, `/ops`, `/techTeam`, `/bmc` — verify view-refresh-label, area-card-button, compact-empty-state, data-card-button, kanban-empty, mktops-tabs render correctly. NO regressions vs current state | Pending Dan | 0d |
| 2026-05-01 | Batch C tech-system-tag-btn cleanup follow-up | Fresh leakage shows `.tech-system-tag-btn` with duplicate-def row (styles.css + marketingOps.css) AND new techTeam.css row. Agent migrated to techTeam.css (single-partial, correct destination) but may not have cleaned both old copies. Quick grep + delete to confirm clean | Pending | 0d |
| 2026-05-01 | Phase 3 CSS Step 2 — Batch D (bmc + registry) | Fastest batch. Migrate bmc.css `.detail-panel*` family → core.css (used by crm). Migrate registry.css `.reg-*` family → core.css (used by knowledge + system-map) | Done — shipped 2026-05-01 21:10 via frontend-builder `adfd43364956ba73e` (238s). 15 classes migrated all copy+delete: 6 detail-panel-* + 4 bmc-canvas/block-* (agent picked up multi-partial scope correctly) + 6 reg-*. core.css 10686→10899 (+212), bmc.css ~618→492 (−126), registry.css ~182→113 (−69). Leakage 373→341 (−32). Build + 1014/1014 + lint clean | 0d |
| 2026-05-01 | Batch D browser smoke test | Hard-refresh `/bmc` (canvas blocks, hero metrics, spotlights, detail panels), `/crm` (detail-panel rows in drilldowns), `/knowledge` + `/system-map` (reg-stat tiles, reg-card grid, reg-link rows), `/marketing-ops` (bmc-canvas/block embedding). NO style regressions vs current state | Pending Dan | 0d |
| 2026-05-01 | Phase 3 CSS Step 3 — retire styles.css safety-net link | After all Step 2 batches land + green leakage check: remove `<link href="/css/styles.css">` from `public/index.html` and `styles.css` entry from `scripts/build-assets.mjs`. Verify lint green. Decision row alongside | Queued | 0d |

| 2026-05-02 | Push 13 commits to origin/main | Local commits `5f48e60`..`a6942b2` + stop-hook update `46fb590` | Done — pushed 2026-05-02 02:05 (`ae9ed09..46fb590`, 14 commits). Vercel auto-deploy triggered. Browser smoke prod still pending | 0d |
| 2026-05-02 | Vercel env-config gaps diagnosed | `/api/google-ads` returns `not_configured` (service-account creds missing). `/api/health` shows `hasAnthropicKey:false` (chat offline, handled by `e690437` banner) and `hasDatabaseUrl:false` (read-model layer falls back to direct Notion/Sheets). Static assets + dashboard/tech-team/daily-sales APIs all verified working on prod. Likely Dan's "local looks more accurate" is browser-tab cache; Incognito test queued | open | 0d |
| 2026-05-02 | `/api/health/details` 500 ENOENT prod bug | `read-model-store.js` `ensureDir()` called `fs.mkdir` without error handling; Vercel Lambda read-only FS threw EROFS, propagating through `Promise.all` in the route → 500 | Done — shipped 2026-05-02 02:25 as `c1aee8b`. Same try/catch + BENIGN_FS_CODES pattern as `570caec` writeJson fix. Verified: prod `/api/health/details` returns 200 with valid JSON | 0d |
| 2026-05-02 | Vercel env vars to set (Dan, dashboard) | 5 vars: `GOOGLE_SERVICE_ACCOUNT_KEY` (unlocks Google Ads + D2C + Calendar), `GOOGLE_ADS_SPREADSHEET_ID=1zJVYckVMX0VBcwgr7VBzkYZ8dSrMPHoDmgpQaRlzH1s`, `ANTHROPIC_API_KEY`, `DATABASE_URL`, `DATABASE_SSL=true`. MCP is read-only for Vercel env config — can't be done from agent session | Pending Dan | 0d |
| 2026-05-02 | Dan prod incognito test | Open https://yds-command-centre.vercel.app in private window to rule out stale browser tab. If still differs from local, name specific element/view (e.g., "tone-coded cards untinted on /dashboard") so it can be diagnosed | Pending Dan | 0d |

Mark previously open loops as DONE this session:
- 2026-04-30 row #10 "Master Spec implementation choice" — separate parallel work track, status row noted as Done already
- v5 backend (excludeStatuses, appliedStatusList, statusBucket Sets, dedup fix, casing fix) shipped + reviewed APPROVED
- v5 frontend (lens / tooltips / banner states) shipped + reviewed APPROVED, awaiting browser test (new row above)
- Daily Sales backend restructure (5-file split + FILE-MAP.md) shipped + reviewed APPROVED
- `.claude/rules/file-discipline.md` written + cross-referenced from workflow.md + decision #75 logged

---

## App Split & Optimization Plan (Proposed 2026-04-08)

### Phase 1: JS Code Splitting (completed 2026-04-08)
- Replace hand-rolled bundler (`scripts/build-assets.mjs`) with **esbuild** (`splitting: true`)
- `npm install --save-dev esbuild`
- **Core bundle (eager):** app.js shell, dashboard, chat, command-shell, overview, toasts, detail-drawer, inline-actions, notion-browser, markdown (~120 KB → ~35 KB gzip)
- **13 lazy chunks (on navigation):** bmc, crm, factory, marketing-ops, ops, tech-team, competitor-intel, projects, claude-usage, documents, team, registry, commitments
- Change `LAZY_MODULE_FACTORIES` (app.js:33-48) → dynamic `import()` paths
- Make `_ensureModule` (app.js:224) async, add `await` in `openNavigationTarget` (command-shell.js:72)
- Change `<script>` tag in index.html to `<script type="module">`

### Phase 2: HTML Partial Extraction (completed 2026-04-08)
- Existing shell partials retained in `public/partials/` (dashboard, factory, marketingOps, ops, bmc, techTeam)
- Extracted inline views: chat, overview, actionQueue, focusArea, team, personView, docs, notion, knowledge, decisions, projects, registry, commitments, crm, claude-usage
- `partialViews` in `command-shell.js` now covers the extracted views and partial loading is cached/re-initialized safely
- `index.html` reduced substantially, with only shared shell/modals/overlays left inline

### Phase 3: CSS Splitting
- Started 2026-04-08:
  `src/css/core.css` now carries the eager shell/shared UI styles
  `src/css/views/{view}.css` now exists for factory, registry, knowledge, marketing ops, tech team, BMC, CRM, ops, and Claude usage
  `public/index.html` now loads `/css/core.css`
  Lazy view CSS is loaded on first navigation via `app.js`
- Remaining work:
  move the rest of the view-specific CSS out of the legacy monolith and retire `src/css/styles.css` as the source snapshot

### Phase 4: Build Pipeline Polish
- Content hashing → `app.[hash].js`, `styles.[hash].css`
- Generate `public/manifest.json` with hashed filenames
- Template hashed names into index.html at build time
- Cache headers: `max-age=31536000` for hashed assets
- esbuild watch mode for dev

### Phase 5: Backend Cleanup (minor)
- Extract cache middleware (server.js:28-66) → `server/middleware/cache-control.js`
- Add partial-specific cache headers for `/partials/*.html`
- Optional: auto-load routes from `server/routes/` directory

### Expected Outcome

| Metric | Before | After |
|--------|--------|-------|
| Initial JS (gzip) | ~80 KB | ~35 KB |
| index.html | 3,924 lines | ~260 lines |
| CSS initial load | 216 KB | ~60 KB core |
| Console errors | 246 | 0 |
| Build time | ~200ms | ~50ms |

### Key Files to Touch
- `scripts/build-assets.mjs` — rewrite bundler
- `src/js/app.js` — dynamic imports, async _ensureModule
- `src/js/modules/command-shell.js` — extend partialViews, await _ensureModule
- `public/index.html` — extract views, change script tag
- `src/css/styles.css` — split into core + views
- `package.json` — add esbuild devDependency
| 2026-05-02 | `.env.example` 6-line patch — Dan to apply manually | Mostly obsolete after Decision #100 hardcoded the 5 IG DB IDs. Only `MCC_POSTS_DB_ID=` line still relevant (Dan to add when MCC Posts DB is created). 5 IG DB ID env-var lines no longer needed | done | 0d |
| 2026-05-02 | Server `NOTION_TOKEN` integration access to "YDS Marketing Hub" page `323247aa-0d7b-81a5-b85d-ce421c959f3b` | MCP integration has access (verified during Phase A — fetched all 5 DB schemas live). Server's `NOTION_TOKEN` is a different integration. Without granting access, all 5 IG readers return `[]` from `.catch()` swallow. Dan to grant access then confirm with one curl | open | 0d |
| 2026-05-02 | Phase C — frontend IG sections in `/marketingOps` view | Dan's pick pending: start now against empty-state shapes, or wait until env confirmed for real-data dev loop. Surface plan: this-week SWPS card, ad-candidates kanban, kill-rule alert widget, weekly ops log latest row, hook + template browsers. ~3-4 hrs frontend-builder + ux-auditor | open | 0d |
| 2026-05-02 | Phase D P0 automations (A1 CC Published → IG Perf row; A2 CC Brand Review → Approvals; A3 Friday cron → Weekly Ops) | NOTION-SETUP.md §6 priority. ~4-6 hrs. Notion webhook → `/api/marketing/notion-event` → dispatch handlers. **Will require splitting `reads/marketing-ig.js` per-DB first** (at 395/400 hard cap, next add MUST split per file-discipline.md). A4–A6 (P1/P2) deferred | open | 0d |
| 2026-05-02 | Schema capture deferred for 5 new IG-playbook DBs | `node scripts/capture-schemas.js` only captures one DB (defaults to Focus Areas). Either extend the script to iterate all registered DBs OR run one-off captures once env vars are set. Per `.claude/rules/api-schemas.md`, future code touching new property shapes should not be merged without a sample. notion-hub.md was updated from live MCP fetches as the authoritative interim source — use that for now | open | 0d |
| 2026-05-02 | scribe doc-pass deferred — `app-reference.md`/`tech-brief.md` don't yet reflect Phase B endpoints | Will batch with Phase C completion. agent-primer auto-regenerates on `npm run build` so it'll pick up the new routes/services on next build. Run scribe explicitly after Phase C UI lands to update tech-brief + endpoint reference together (avoids two scribe spawns in one work-stream) | open | 0d |
| 2026-05-02 | `reads/marketing-ig.js` at 395/400 hard cap — Phase D additions MUST split first | Per file-discipline.md "next change MUST be a split — no exceptions." Split structure ready: one reader function per DB → one file per DB (`reads/ig/performance.js`, `reads/ig/hook-patterns.js`, `reads/ig/templates.js`, `reads/ig/approvals.js`, `reads/ig/weekly-ops.js`) + index re-export shim to preserve public API | open | 0d |
| 2026-05-02 | Phase C architecture pick — new `/ig` view OR embed in `/marketingOps` | Dan picked new view (Decision #99). Frontend-builder shipped 6 files, lint+tests clean | done | 0d |
| 2026-05-02 | Phase C `/ig` view shipped — Dan manual UI test pending before reviewer/auditor dispatch | New view at sidebar Growth → "IG Playbook". Test list: header subtitle, env-empty banner, SWPS hero color, kill-rule banner, hook/template expand toggles, posts table SWPS tone + ✓/✗ Hit Target, refresh button, mobile drawer. After Dan confirms render is correct, dispatch code-reviewer + ux-auditor in parallel | open | 0d |
| 2026-05-02 | Phase C frontend layout choice — kill-rule + SWPS hero side-by-side; "Clear" card placeholder when no triggers | Frontend-builder added a "Clear" card sibling so the 2-col grid stays balanced when killRuleTriggerCount=0. Logged for reviewer awareness — minor visual polish, not a functional concern | open | 0d |
