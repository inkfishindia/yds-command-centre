# Decision Register — Command Centre

| # | Date | Decision | Rationale | Status |
|---|------|----------|-----------|--------|
| 1 | 2026-03-21 | Only map spreadsheet IDs when evidence is exact — never guess strategy wiring | Incorrect wiring causes silent data corruption in dashboards | Active |
| 2 | 2026-03-21 | Keep Google Sheets as short-term source of truth (no immediate Notion migration) | Sheets already working, migration would stall feature dev | Active |
| 3 | 2026-03-21 | Use service-account auth for Sheets, not browser OAuth | Backend-only access, no user-facing Sheets flows needed | Active |
| 4 | 2026-03-21 | Redesign BMC as hybrid executive canvas (classic BMC + dashboard + detail drawer) | Plain BMC too static for CEO daily use; needs interactivity | Active |
| 5 | 2026-03-21 | Build marketing content features into Command Centre, not as separate MCC app | Reduces context-switching, leverages existing infrastructure | Active |
| 6 | 2026-04-08 | Fix frontend x-show→x-if for lazy module views | Alpine evaluates all x-show expressions on load including uninitialised lazy modules, causing 246 errors and empty panels; x-if removes from DOM until active | Pending |
| 7 | 2026-04-11 | Keep ERP as primary UI; wire to CC + Live Dashboard backends via API layer | ERP has better UI/UX (React+Tailwind, 78 pages); CC and LD already have the data infrastructure. Avoids duplicating backends. | Active |
| 7 | 2026-04-08 | Reconcile conflicting Stop hooks into single per-action format | Two Stop hooks had contradictory requirements (one summary row vs per-action rows) causing infinite loop at session end | Active |
| 8 | 2026-04-08 | Use esbuild for JS code splitting (not Vite/Webpack) | Zero-config, single dependency, 50ms builds, stays within Alpine.js stack — no framework change needed | Proposed |
| 9 | 2026-04-08 | Diagnosed live "data not loading" as auth-gate cookie encoding regression in commit 4979625; no code change yet | URL-encoding cookie on set + decodeURIComponent on read locks out users holding pre-deploy raw cookies until re-login. Vercel logs confirm all API endpoints return 200 after fresh login | Pending fix |
| 10 | 2026-04-08 | Added CRM_CONFIG_SPREADSHEET_ID and OPS_WAREHOUSE_SPREADSHEET_ID to Vercel Production env; deferred STRATEGY_SPREADSHEET_ID | Two were missing from prod but present in local .env causing silent data gaps. STRATEGY value doesn't exist locally — needs clarification from Dan before adding | Active |
| 11 | 2026-04-08 | Fix dashboard nav badge null-deref with optional chaining (not x-if wrapper) | Alpine evaluates x-text independently of x-show; old guard relied on x-show but x-text still threw. Optional chaining is smallest surface-area fix — one line in public/index.html:105 | Active |
| 12 | 2026-04-08 | No code change for "exports is not defined" error in notion-browser chunk — diagnosed as stale browser cache | Local + git-committed chunk is pure ESM with zero exports references. Dan's commit ffe0fef already fixed cache headers (app.js no-cache, hashed chunks immutable), so hard refresh resolves it and prevents recurrence | Active |
| 13 | 2026-04-10 | Bypass Supabase auth on yds-live-dashboard instagram pages using service-role admin client | Quick dev bypass to unblock instagram pages — RLS blocks unauthenticated queries. Proper user auth to be added before team rollout | Active |
| 14 | 2026-04-11 | Add x-api-key header bypass to CC auth-gate for ERP service-to-service calls | ERP proxy needs to call CC API without browser cookie session; API key is simplest secure pattern for internal service auth | Active |
| 15 | 2026-04-11 | Wrap ERP app root in ErrorBoundary component | No error boundaries existed — any component crash caused full white screen with no recovery option | Active |
| 16 | 2026-04-11 | Orders: Sheets for operations, Supabase for analytics — don't merge | Sheets has operational details (status, fulfillment, customer info); Supabase has financial/analytics (revenue, margins, attribution). Different purposes. | Active |
| 17 | 2026-04-11 | Campaigns: Notion/CC for management, Supabase for ad attribution only | Notion has rich campaign planning metadata; Supabase `dim_campaign_map` links UTMs to ad IDs. Complementary. | Active |
| 18 | 2026-04-11 | Instagram: Supabase/LD for ops page, Notion/CC for editorial calendar | Supabase has post-level operational data and performance views; Notion has editorial planning. Not duplicates. | Active |
| 19 | 2026-04-11 | Monorepo: merge ERP into CC repo, kill yds-erp and absorb yds-live-dashboard | 3 separate apps is too much to manage. ERP has the best UI (React, 78 pages). CC has the backend. LD's Supabase data can be called directly. One repo, one deploy. | Active |
| 20 | 2026-04-22 | Build System Map as single `/api/system-map` aggregate endpoint, not per-section routes | Simpler to reason about, agents/clients get atomic snapshot, 60s cache amortises fs scan cost across bursts | Active |
| 21 | 2026-04-22 | System Map exposes `idPresent` boolean for Sheets, never raw spreadsheet IDs | Read-only inventory endpoint must not leak secrets; test asserts no field >12 chars on sheet entries | Active |
| 22 | 2026-04-22 | Gitignore `data/schemas/*.json` captured API samples; commit only the rule + README | Samples contain live Notion page titles, Sheet row values, PR text — not safe to commit. Regeneration is one command (`node scripts/capture-schemas.js`) | Active |
| 23 | 2026-04-22 | Wire `.claude/rules/api-schemas.md` into backend-builder / tester / code-reviewer only, not all agents | Only those three write or review Notion/Sheets/GitHub parsing code. Adding to others would dilute setup without behaviour change — Alpine templates consume already-simplified keys | Active |
| 24 | 2026-04-22 | Rejected Next.js+React, Python SDKs, Clasp, and generic Notion-API-docs from a "add these to your stack" suggestion list | Next.js/React violates Alpine.js-only rule; Python splits Node-only server; Clasp is for `.gs` scripts inside Sheets (wrong pattern vs. Node `googleapis` SDK); generic Notion docs weaker than workspace-specific `notion-hub.md` | Active |
| 25 | 2026-04-23 | Dan ↔ Colin Queue uses existing dark theme, not the `#FAFAFA` light palette in the source spec | Light island inside a dark app causes context loss / eye strain; section hex values mapped cleanly onto existing `--amber` / `--red` / `--accent` / `--text-secondary` tokens with only one new `--amber-muted` added | Active |
| 26 | 2026-04-23 | Drop-create routes through the same approval gate as answer-update | Consistency with "all writes gated" rule beats marginal self-approval friction; auto-approve can be layered on later if it becomes annoying | Active |
| 27 | 2026-04-23 | Voice + attach on drop composer rendered as disabled stubs, not removed | Spec calls them out; preserving the surface fidelity avoids future re-layout when transcription infra lands. Tooltip reads "coming soon" so intent is clear | Active |
| 28 | 2026-04-23 | Paused Dan ↔ Colin pipeline before `ux-auditor` + `tester` — Dan tests UI manually first | Dan's feedback: UX audit on code he hasn't loaded is wasted compute; integration tests against a just-shipped view often test wrong things before manual validation | Active |
| 29 | 2026-04-23 | Drop `Read CLAUDE.md` from 7 agent pre-flight blocks — agents get CLAUDE.md via `claudeMd` system context already | Re-reading it was pure waste (~172 lines × every spawn). Pre-flight should only list reads that aren't in the auto-injected context: the primer, domain rules, and files the agent is about to edit | Active |
| 30 | 2026-04-23 | Document `SendMessage` continuation pattern in CLAUDE.md Dispatch rules instead of making it implicit convention | Orchestrators were respawning review agents per pipeline step, paying the pre-flight cost each time. Making the pattern explicit + giving a worked example (code-reviewer: spawn once for backend diff, SendMessage the frontend diff) ensures the first-spawn cost is amortised across the session | Active |
| 31 | 2026-04-23 | Corrected hardcoded Dan↔Colin Queue DB ID from `00969f07-8b4d-4c88-8a45-ec1e95b3bacb` to `43d71386-85a1-4582-a2dd-6d541bdcc5d3` | Original ID was mis-transcribed during yesterday's build — schema-verify step passed (a DB with matching schema did exist somewhere) but the ID itself pointed at nothing Rube could see. Real DB surfaced via Notion `/v1/search` query. Tightening rule: schema verification alone is insufficient — also run a live row query against the target ID before committing a hardcoded constant | Active |
| 32 | 2026-04-23 | Dan ↔ Colin v2 spec published as `dan-colin-queue-v2.md` (deltas-only) instead of rewriting v1 | v1 stays authoritative for unchanged specs (typography base, animations, layout grid, approval flow); v2 documents only the 10 improvements. Keeps spec surface small and diffable, frontend-builder reads both | Active |
| 33 | 2026-04-23 | Use Alpine `:class="dcAnswerFocused && 'dc-fab--hidden'"` to hide FAB on answer focus, not the spec's pure-CSS `:has()` selector | Spec assumed `.dc-fab` was descendant of `.dc-body` — it isn't (sibling), so `:has()` was a silent no-op. Alpine state binding works and gives future state-awareness (e.g. delayed reappear). Tightening rule: CSS-only selectors that assume DOM structure must be validated against the template before speccing | Active |
| 34 | 2026-04-23 | Resolved design-planner's 5 open questions with orchestrator defaults rather than blocking on Dan | Dan's standing "use your judgment" rule + questions were all low-stakes UX polish (scroll+expand, hash determinism, keyboard scope, cheatsheet placement, stale threshold). Defaults captured in the frontend-builder brief so audit trail is preserved; Dan can override on manual test | Active |
| 35 | 2026-04-23 | Identified repo collision: two unrelated apps (Alpine.js Colin/Notion v1.0.0 at `@yourdesignstore.in/.../market/yds-command-centre` and React ERP v2.0.0 at `~/Projects/yds-command-centre`) both pushing to GitHub `inkfishindia/yds-command-centre`. Recommended separation onto two repos; no push executed | Diverged at `Initial commit` (8bab433) on April 8 into two parallel lines that never merged. Pushes from either clone reject the other because file structures, package.json, and stacks are fundamentally incompatible. Cannot resolve via rebase/merge — must split into separate repos before today's Alpine.js work (`e672039` local-only) can go live anywhere | Proposed — pending Dan confirmation which app is canonical on Vercel |
| 36 | 2026-04-23 | From Dan's ERP reference app, ported three features into CC: (1) System Map "Views & Status" registry, (2) UTM Builder in Marketing Ops, (3) Activity Feed on Dashboard merging decisions + commitments + Dan↔Colin resolutions. Skipped Order/Production/Kanban (FactoryOS), AI Studio pages (covered by `pixel` + `/commission`), Google Workspace contexts (MCP), PPC Calculator (exists elsewhere), Portfolio drill-down (duplicates Focus Areas) | Picked on signal-to-effort: small high-value surfaces that filled real gaps (module honesty, campaign URL tagging, unified activity). Skipped either covered-already or FactoryOS-domain. Code-reviewer verdict PASS WITH NOTES across 14 gates, 784/784 tests | Active |
| 37 | 2026-04-23 | Diagnosed `/api/overview` 500 on Vercel production as ENOENT from `server/services/read-model-store.js` writing to read-only bundled filesystem (`server/data/read-models/*.json`). No fix committed yet — Dan to choose Option A (graceful ENOENT/EROFS swallow in `writeJson`, ~10 lines) vs Option B (redirect `DATA_DIR` to `/tmp` on Vercel, ephemeral cache only) | Postgres isn't wired on Vercel (`DATABASE_URL` not set → `db.isDatabaseEnabled()` returns false), so both fs write and DB fallback fail → catch block in `base.js:buildPersistedReadModel` re-throws. Same failure mode likely affects other read-models (dashboard, crm, ops, marketing-ops, tech-team, action-queue) but symptoms are masked by warm-lambda memory cache | Pending Dan choice |
| 38 | 2026-04-25 | Split Vercel build in `vercel.json` into `@vercel/static` for `public/**` + `@vercel/node` for `server.js`, with explicit routes for `/css/`, `/js/`, `/partials/`, favicons ahead of the catch-all `/(.*)` → server.js | Single `@vercel/node` builder was auto-transpiling browser bundles (`public/js/*.js`) from ESM to CJS, producing `require()` calls that the browser can't execute → blank page. Static builder serves the bundles untouched. Chosen over `"type":"module"` in package.json (would break CJS server) and renaming to `.mjs` (touches build script + every chunk reference) | Active |
| 39 | 2026-04-25 | Reuse existing Dan↔Colin Queue (`createDrop()` + `Section=📥 Drop, Owner=Colin, Status=Open`) as the offline-chat fallback inbox instead of building a new "Chat Inbox" DB | Backend service + endpoint + Notion DB schema already exist and are tested. `Owner` field already separates Colin's outbound questions to Dan from Dan's inbound drops to Colin. Net cost: zero new infra. If drops get noisy and the semantic mixing actually costs Colin time, easy to split later — the queue rows have enough metadata to migrate selectively | Active |
| 40 | 2026-04-25 | Defer per-DB structured forms (commitment form with owner+due+DoD pre-fields, decision form with rationale+status, etc.) for v1 of the offline chat composer — ship free-text textarea only | Per-DB forms add a maintenance surface (every form must stay in sync with Notion schemas) for marginal benefit, since Colin can structure free-text drops himself when he processes them. Promotes shipping over speculation. Will revisit if Colin's review of the drops surfaces "I keep manually re-asking Dan for the same metadata" patterns | Active |
| 41 | 2026-04-25 | Fix dan-colin view-leak with surgical `<template x-if>` → `<div x-show>` swap on dan-colin only (commit `ed520fb`); leave the same latent bug in the other 16 partial-loaded views unfixed | Dan-colin is the only partial with `position:fixed` element (the FAB) at root, so the only one whose leak is visible. Other 16 (team, factory, marketingOps, bmc, crm, techTeam, ops, registry, status, knowledge, projects, decisions, docs, system-map, claude-usage, commitments) have same latent bug but no symptom. Surgical fix avoids 16-view regression risk. Documented as known-but-deferred — convert reactively when any of those views grows position:fixed children | Active |
| 42 | 2026-04-28 | Daily Sales registers two tabs (`SALES_YTD` static `2026` + `SALES_CURRENT_MONTH` dynamic `"<MonthName> YYYY"`) instead of a single combined source | Reflects how Dan actually maintains the workbook (cumulative year tab + rolling monthly tab). Dynamic resolution lets the system auto-rotate on the 1st of each IST month with zero code change. Sparse `2026` tab still gets first-class treatment because Dan plans to populate it | Active |
| 43 | 2026-04-28 | Extended `sheets.js` `SHEET_REGISTRY` to support `sheetName` as either string or `(now) => string` function — first registry entry to use this pattern | Avoids hardcoding monthly tab names that break on Apr→May rollover. The function form is opt-in (existing 25+ static entries unchanged) and accepts an injectable `now` so tests stay deterministic and the IST-aware caller can pass a TZ-shifted Date. Cheap surface-area extension; one `resolveSheetName(entry, now)` helper threaded through `fetchSheet`/`getSheetHeaders`/`appendRow`/`updateRow` | Active |
| 44 | 2026-04-28 | YTD aggregation deduplicates the union of YTD + month tab rows (month wins on Order # collision) — chosen over "YTD reads YTD tab raw" | Reviewer-flagged blocker: with raw-YTD, the same Order # appearing in both tabs with different amounts caused YTD ≠ MTD ≠ trend30d, breaking the founder's mental model that "totals tie out." Dedup-then-aggregate makes all three views consistent. Test added covering the overlap case explicitly | Active |
| 45 | 2026-04-28 | All date math in daily-sales-service uses explicit `Date.UTC(y, m-1, d)` arithmetic, never `Date.parse()` or `new Date(string)` | `Date.parse` on `YYYY-MM-DD` is locale-sensitive across runtimes (UTC midnight on most, but historically not all) and `new Date('28-02-2026')` mis-parses to invalid or NaN. Explicit UTC math is portable and self-documenting. IST bucketing layered on top via `getISTNow()` (UTC + 5h30m, then read UTC components) — server runs UTC, business runs IST, "today" must mean Indian today | Active |
| 46 | 2026-04-28 | Daily-sales partial uses `<g x-show>` on SVG groups proactively, not `<template x-if>` — applied at build time, not retrospectively as on the 16 other views (#41) | Same lesson as decision #41 (view-leak from x-if on dynamically-injected partials), but applied before shipping. Reviewer caught three x-if instances in the SVG trend chart (gridLines, xLabels, hoverCols) — fixed with `<g x-show>` swap. Confirms the pattern is now part of the team's mental model: if it's a partial-loaded view, default to x-show on the conditional wrapper | Active |
| 47 | 2026-04-29 | Daily Sales YTD treats tab name as **Indian FY end-year** (`2026` = FY 25-26, ended Mar 2026; `2027` = FY 26-27, current). `SALES_YTD` resolves dynamically (`m >= 3 ? y+1 : y` against IST date); new `SALES_LAST_FY` registry entry resolves to `currentFYEndYear - 1`. Display label derives "FY 25-26" / "FY 26-27" from tab name. New `ytd.vsLastFY` delta uses **FY-ordinal like-for-like filter** (April 1 = FY day 1) so prior FY is sliced to today's FY-day-of-year, not calendar day. Auto-rotates April 1 IST | Static `SALES_YTD = '2026'` was reading last closed FY as if it were YTD. Dan operates on Indian FY for all reporting/statutory work — calendar-year tab is incoherent with the rest of his data model. Like-for-like filter is FY-ordinal not calendar-ordinal because day 1 of FY = April 1, so prior FY's "same period" must use the same ordinal anchor | Active — shipped with `vsLastFY` showing FY 26-27 vs FY 25-26 day-29: +160 orders (+16%), +₹18.4 lakh (+69.1%), AOV +₹1,223 (+45.8%). Dan dropped the monthly-tabs-only proposal in favor of consolidate-as-you-go (#48) |
| 48 | 2026-04-29 | Daily Sales workflow: **consolidate-as-you-go** (one rolling month tab + cumulative FY tab + monthly transfer ritual) over **monthly-tabs-only** (12 permanent tabs/year derived to FY total at runtime) | Dan flagged "becomes too many tabs / i would like to consolidate them as we go" against the monthly-tabs-only proposal. Trade-off accepted: ~2 min of manual work each month-end (copy April rows → append to `2027`, rename tab to May, clear data rows) in exchange for permanently 2 active tabs (current month + FY cumulative) plus archive. Backend's Order# dedup handles the transfer-window overlap safely (current-month wins on collision = freshest data wins). Same architecture supports either workflow — the choice is ergonomic, not technical | Active |
| 49 | 2026-04-29 | Skipped `code-reviewer` and `frontend-builder` agent dispatches for the FY frontend changes; main session edited the partial directly | Frontend changes were template-level: 3 stat cards gained delta blocks copied from existing MTD `vsLastMonthSameDate` pattern, period label gained an `x-text` binding, unavailable notice swapped raw tab name for display label. Risk surface is tiny — new bindings use `?.` chains and `x-show` guards consistent with the rest of the partial. Live API verification + 44/44 backend tests + Dan's pre-approval served as the gate. Documented as a precedent: when changes mirror an existing, reviewed pattern in the same file, agent dispatch overhead exceeds the value | Active — flag for retroactive review if anything misbehaves on Dan's browser test |
| 50 | 2026-04-29 | Refactor `daily-sales-service.js` from monolith into 6-module folder (`index`/`parse`/`filters`/`aggregations`/`data-quality`/`fy`) with normalized `Order` schema as the long-term contract. Backward-compat shim at old path | Phase 3 (Postgres mirror, Metabase, Observable Plot) requires a clean separation between row-source, parsing, filtering, aggregation, and data-quality. Without it, swapping Sheets→Postgres later means rewriting the service. With it, only `parse.js` (and a new `db-source.js`) change. Cost: one refactor pass; benefit: Phase 3 becomes additive not destructive. The `Order` schema is the contract — Phase 3 consumers (Metabase / direct API hits) all go through it | Active |
| 51 | 2026-04-29 | Realized filter (`Status ∈ {Delivered, Fulfilled, Partially Fulfilled, Fullfilled-typo}`) is **default-on** for financial aggregations (today/MTD/YTD/trend/topStates/concerns/mix-by-channel/orderType/payment/print), but **status mix breakdown stays unfiltered** | Dan's stated intent: "to get accurate realized sales figures." Default-on means honest numbers without an extra click. Status mix kept unfiltered because it IS the diagnostic — filtering it would hide the cancellation/draft volume that justified the filter in the first place. Reviewer flagged this divergence as subtle; left a code comment so future maintainers don't "fix" it. The realized filter accepts `'Fullfilled'` (with typo) because Dan's source data has 260 such rows | Active |
| 52 | 2026-04-29 | Filter cache is bounded LRU (50 entries) keyed by hashed FilterSpec + IST date bucket | With 8 filter dimensions the unbounded cache grew unboundedly per filter combination. 50 entries handles realistic CEO-usage patterns (~10–20 distinct slices) with headroom; on overflow, oldest evicts. 5-min TTL preserved. Map preserves insertion order so eviction is `_cacheMap.keys().next().value`. LRU-touch on read keeps frequently-accessed slices warm. Was a reviewer 🔴 blocker fixed in second backend pass | Active |
| 53 | 2026-04-29 | Backend emits **both** wide format `trend30d: [{date,orders,revenue}]` AND long format `trend30dLong: [{date,metric,value}]`. Frontend uses wide; long is reserved for Phase 3 chart-lib swap | Observable Plot / ECharts / Vega-Lite all consume long format natively. Emitting both costs nothing (derivation is trivial), keeps current frontend untouched, and means swapping the SVG sparkline for Plot is a one-line frontend data-source change with zero backend work. Comment in `_dsComputeTrendPaths()` explicitly references `trend30dLong` and the architecture doc so the swap is signposted | Active |
| 54 | 2026-04-29 | ESC key handling on the daily-sales partial uses Alpine's `@keydown.escape.window` directive on the wrapper element, NOT manual `document.addEventListener('keydown', ...)` | Reviewer flagged the manual-addEventListener pattern as a memory-leak risk on partial-reload. The Alpine directive ties the listener to the element's lifecycle — when the element ever leaves the DOM, Alpine removes the listener automatically. Idiomatic, zero bookkeeping. Same pattern should be applied to other partials that have global keydown handlers (none today, but the precedent is set). The wrapper's `display:contents` style means it doesn't render but still hosts events — necessary because the directive needs an element to attach to | Active |
| 55 | 2026-04-29 | Filter chip values render dynamically from `filters.available.<dimension>` (the API's discovered values), never hardcoded enums in the partial. `(unknown)` is filtered out of chip lists so it only appears in mix breakdowns | Found Order Type pills hardcoded to `B2C / B2B` — but actual schema is B2C / DS / Manual / Stores (no B2B). Hardcoded enum was wrong AND would silently fail to surface DS/Manual/Stores once Dan tags those rows. Pattern was already established for paymentMode pills but not extended; now both follow the same rule. Future filter dimensions (status, channel) should use the same dynamic-from-API pattern. Consequence: backend MUST keep `filters.available.<dimension>` populated and stable — it's a contract, not an internal detail | Active |
| 56 | 2026-04-29 | Sheet column normalizers (`parse.js:normalizeOrderType`, also any future `normalize*` helpers in this layer) are **data-defined, not enum-defined** — pass through any non-empty trimmed value, collapse only true blanks to `(unknown)`. Same rule for filter parsers (`filters.js:parseFilterSpec`) — accept any value verbatim, do case-insensitive comparison in `applyFilters` not at parse time | Found `normalizeOrderType` whitelisted only `'B2C'` / `'B2B'` and dumped everything else into `'(unknown)'`. Filter spec parser similarly whitelisted only `B2C/B2B/all`. The dashboard was reporting 647 "unknown" Order Type orders worth ₹36.87 lakh — but the source sheet had clean values: B2C 512 / DS 510 / Manual 102 / Stores 39. **Whole "data quality issue" was code, not data.** Lesson: enum whitelists in normalizers are an anti-pattern when the upstream source is human-edited (sheets, CRM forms, etc.) — categories evolve faster than code. Tighter rule: any normalizer that has an `if (val === 'X') return 'X'` ladder should be replaced by `return trimmed || '(unknown)'` unless there's a specific business reason to coerce/reject. Companion to #55 (frontend) — both layers must be data-defined for the contract to hold | Active — applied to `normalizeOrderType` + `parseFilterSpec.orderType`; should be retroactively applied to any other normalizer in `parse.js`/`filters.js` that has the same whitelist pattern (none found today, but watch for it) |
| 57 | 2026-04-29 | Sheet↔dashboard parity check is a permanent diagnostic at [scripts/probe-sheet-parity.js](scripts/probe-sheet-parity.js); `filters.available.*` is **intentionally scoped to current FY + current month tabs only**, NOT to the historical FY archive (`2026` tab is read for `vsLastFY` aggregation but does not feed `filters.available`) | Built the parity probe in response to "should we check all 3 tabs and all fields?" — generalizes the earlier targeted Order-Type probe to 7 columns × 3 tabs. Surfaces parser bugs, dedup bugs, and stale API entries. First run confirmed parser fixes (#56) work + revealed `Status="Lost"`, `Payment Mode="Cash"/"Cheque"`, and 130+ Shipping State variants live only in the closed `2026` archive tab. These show as 🔴 in the script BUT are correct behavior — historical-FY filtering is out of scope until Postgres + Phase 3. Documented the scope choice here so future parity runs are interpreted correctly: 🔴 from values existing only in `2026` tab = expected; 🔴 from values present in current-FY tabs but missing from API = real bug. Reusable verification prompt logged in handoff for paste-into-future-sessions. The script also exposed real data-hygiene issues (Tags column 200+ variants, Shipping State spelling variants, COMMMERCE typo) — those are source-data fixes, not code | Active — script is canonical for future verification; run after any change to `parse.js`/`filters.js`/`aggregations.js` |
| 58 | 2026-04-29 | All cross-period delta computations in `daily-sales/index.js` (`vsYesterday`, `vsLastMonthSameDate`, `vsLastFY`) MUST be apples-to-apples: both sides go through `applyFilters(orders, activeFilter)` before aggregating. Order-of-operations: compute `*AggFinal` (filtered) first, then derive deltas from those — never mix unfiltered `*Agg` with filtered `*AggFinal` | Original `vsLastFY` block ran BEFORE the filter pipeline, computing `delta(ytdAgg.X, lastFYAgg.X)` from gross orders. The displayed YTD strip used `ytdAggFinal.X` (filtered/realized). Result: shown YTD = 934/₹20.74L but delta = +160/+69.1% computed against gross 1159/₹45.12L. The math literally did not tie out (delta 160 ÷ (934−160) = 20.7%, but pct shown was 16% from the gross prior 999). Story flipped after fix: realized-vs-realized FY 26-27 vs FY 25-26 same period is **+25 orders / -17.7% rev / -19.9% AOV** — actual revenue contraction, not the +69.1% gross-vs-gross illusion. Lesson: any "current" and "prior" pair shown in the same UI card must come from aggregates that share filter state. Vigilant: when adding new period comparisons, place delta computation AFTER all filter applications and use the `*Final` aggregates exclusively | Active |
| 59 | 2026-04-29 | "Today" stat is the only card that exposes BOTH realized and gross — `today.allStatuses: { orders, revenue, aov, vsYesterday }` lives next to the existing realized fields. MTD and YTD intentionally remain realized-only (financial reports). Frontend shows the gross count as a quieter secondary line under each Today stat, with `x-show` guard `(allStatuses.X ?? 0) > (today.X ?? 0)` so the line vanishes when everything is realized | Realized filter excludes `Order Placed` (142 rows MTD, ~45% of recent orders) + `Processing` (65) statuses. Today's orders are mostly still in those pre-shipment statuses → `today.orders` shows 0 by lunchtime even when 12 orders came in (live data at fix time). For "Today" the user wants operational glance ("did orders happen?"); for MTD/YTD they want financial accuracy. Asymmetric exposure is the right call — financial reports stay clean of unrealized inflation, while the at-a-glance card surfaces operational reality. Wording: "12 placed today" / "₹61,369 placed today" / "₹5,114 avg placed" — "placed" maps to Dan's mental model better than accounting jargon ("unrealized"). Backend cost is ~zero (gross aggregates already computed; just surfacing them) | Active |
| 60 | 2026-04-29 | `vsLastMonthSameDate` source pool is `[...allOrders, ...lastFYOrders]` — concat without dedup, because the two pools are disjoint by date (current-FY tabs are April YYYY+1 onward; lastFY tab is April YYYY–March YYYY+1). The union exists specifically to handle the FY rollover case (April → March crosses FY boundary) without special-casing | Pre-fix `lastMonthMTDOrders` filtered only `allOrders`, which meant April's vs-last-month was permanently null/empty (March lives in the closed-FY archive `2026` tab). Could have special-cased `if (todayMonth === 'April') include lastFYOrders` but that's brittle and grows. Always-union approach: filter by date-range does the scoping; for non-April months, the lastFY rows simply don't match the date filter (no-op cost). For April, March rows from `2026` are included naturally. Trade-off: pools must remain disjoint by date — if someone misfiles April rows into the `2026` tab, this would double-count. Acceptable given month-wins dedup elsewhere catches misfiling at the parser level. Decision #57's scope rule (filters.available scoped to current FY only) still holds — `lastFYOrders` is used internally for lookback aggregations, never exposed to filter pickers | Active |
| 61 | 2026-04-29 | Daily Sales redesigned around batch-import reality (Yesterday-as-hero, v4) instead of pursuing more frequent imports (path B). v4 swaps Today→Yesterday in the hero strip, demotes Today to a small "↻ N placed since refresh" pill in a new sticky freshness banner, and stamps "Data through {yesterday.date}" prominently at top | Sheet is batch-imported once daily (evening/night IST). At 11am, Today is empty by design — but v3's Today-as-hero made that read as "broken." Path A (UI reframe) makes the dashboard truthful about what it is — a daily-cycle ops view — without depending on infra changes. Path B (every-2-4h imports) is a real upgrade but requires touching whoever owns the import script + Google API rate-limit risk + still has early-morning lag. Dan picked A. Backend cost minimal: `yesterday` block + `freshness.dataCutoff` field. Frontend adds `.ds-freshness-banner` (5 classes) and 3 date helpers. Reuses all v3 dual-metric stat-card classes | Active |
| 62 | 2026-04-29 | Banner "Data through {date}" anchors to `yesterday.date`, not `freshness.dataCutoff`, with fallback chain `yesterday.date \|\| dataCutoff` for defense | dataCutoff = "latest istDateKey with any orders" — which can be today (e.g. if today has 12 partial-day orders, dataCutoff = today). Yesterday hero card binds to `yesterday.date` (= today minus 1, always). If banner used dataCutoff, it would read "Data through Apr 29" while hero shows "Yesterday · Apr 28" — internally contradictory. Anchoring to `yesterday.date` keeps the user-facing cutoff stamp coherent with what's shown. dataCutoff stays in the API surface for future stale-import detection (when `dataCutoff < yesterday.date`, last night's import didn't run). MTD/YTD label appendices use the same fallback for consistency | Active |
| 63 | 2026-04-29 | Inline `style="color: var(--text-secondary); margin-left:2px"` on MTD/YTD delta-suffix spans (6 instances, pre-existing from v2) harmonized to `.ds-stat-delta-suffix` class as part of v4 work — closing a consistency gap I flagged but deferred during v3 review | Originally left out of v3 scope to keep that PR tight. v4 already touches the MTD/YTD label rows (adding "· through {date}" appendix), so the harmonization rides along at near-zero marginal cost. Tokens-only-via-classes is a hard constraint for the project; remaining inline styles in the partial are now scoped to filter-strip empty states + one mix-grid empty state, both structurally one-off. Pattern reinforced for future: when touching adjacent code, prefer to close known gaps rather than defer indefinitely | Active |
| 64 | 2026-04-29 | Add "↗ Open in Sheets" link button to all sheet-backed views (Daily Sales, D2C, CRM, BMC, Competitor Intel, Ops, Tech Team, Dashboard). Deep-link to specific tab gid, not spreadsheet root | Dan asked "for all sheets pages, can we add a link button to open the sheet?" and confirmed scope = all + deep-link variant. Deep-link is more useful (lands on the right tab) at cost of one cached metadata call per sheet — negligible after warm. Implementation: add `getSheetUrl(sheetKey)` helper in `server/services/sheets.js` (resolves gid via Sheets metadata API, caches), each sheet-backed route returns `meta.sheetUrl` + `meta.sheetName`, frontend adds one reusable `.btn-open-sheet` class placed in each view header | Active |
| 65 | 2026-04-30 | Daily Sales hero/stat-strip type scale unified at **24px desktop / 22px mobile** for BOTH `.ds-stat-value` (MTD/YTD) and `.ds-stat-metric-value` (Yesterday primary dual-metric card). Resolution scales metric variant UP to match, not MTD/YTD DOWN | The v3-flagged mismatch (22 vs 24 on desktop) had `.ds-stat-metric-value` missing a `@media (min-width: 768px)` rule. Yesterday is the hero per v4, so anchoring its primary metric at the smaller scale would fight the narrative. 24px also reads better against the dense secondary-metric labels and delta-suffix spans surrounding each value. Cost: trivial (one media-query rule added). Rule for future stat cards: anchor to `.ds-stat-value` desktop scale and write the matching media block when defining a new variant | Active |
| 66 | 2026-04-30 | View CSS lazy-load `_viewStyleFile.fileMap` keys MUST equal the actual view name string used by `view ===` in index.html and passed to `_loadPartial(name)`. The fileMap is lookup, not aliasing | Commit `584441e` mutated 5 keys to camelCase (`'dan-colin'` → `danColin`, etc.), silently breaking CSS load for Dan-Colin, Daily Sales, System Map, System Status, Claude Usage. The pages rendered as raw HTML for ~1 day before Dan caught it on a reload. Reverted to kebab-case (`'dan-colin'`, `'daily-sales'`, `'system-map'`, `'claude-usage'`, `status` for system-status). Convention: when adding a view, its key in `_viewStyleFile.fileMap` and `_partialViewClass.classMap` MUST be identical to the view-name string and identical to each other. Defense: a unit test asserting `Object.keys(fileMap).every(k => k in classMap)` would have caught this in CI — belongs alongside other frontend health tests | Active |
| 67 | 2026-04-30 | Stack-reference docs (Alpine.js, Express routing, Apache ECharts vanilla, Chart.js, Preline UI) become canonical reading via new [`.claude/rules/external-references.md`](.claude/rules/external-references.md), auto-loaded for both frontend (`public/**`, `src/js/**`, `src/css/**`) and backend (`server/**`, `server.js`) agents via `paths:` glob. Tailwind is **not** adopted — Preline is reference-only, snippets must be ported to existing CSS tokens. Adding ECharts or Chart.js to a view requires a follow-up decision row first | Older training data may pre-date current Alpine 3.x directive list, Express 5 async-error semantics, or ECharts 5.x option shapes — when doc and model disagree, doc wins. The repo already has hand-rolled dark-theme CSS + design tokens; bolting Tailwind on would fragment the styling system and break the design pipeline (`design-planner` → `frontend-builder`). One chart lib per view is enforced to keep bundle weight + cognitive overhead bounded. Pointer added to both `frontend-patterns.md` and `server-patterns.md` so the rule surfaces in normal agent invocations without a separate Read | Active |
| 68 | 2026-04-30 | Daily Sales lens segment-control: three named lenses **Realized** / **Active** / **All** (1-of-N pill row) above the hero strip, with auto-detected "Custom" indicator when user hand-edits chips off-lens. Lens-to-filterSpec mapping: Realized = `{status:'realized', excludeStatuses:[]}`, Active = `{status:'all', excludeStatuses:['Cancelled','RTO','Lost','Draft Order']}` (i.e. Realized + In Flight), All = `{status:'all', excludeStatuses:[]}`. Lens click overrides any custom chip state without warning. URL hash uses `lens=` param when on a named lens; explicit `status`/`excludeStatuses` when custom | Lens is a 1-of-N viewing mode (operational vs financial vs gross); slice is multi-select chips. Mixing them muddles the user's mental model — a control that's both "switch view" and "filter to subset" reads ambiguously. Three lenses cover the high-frequency questions ("what did we earn?" / "what's in pipeline?" / "what came through the door?"); everything else stays in the filter strip below. Backend stays generic via `excludeStatuses: string[]` — does NOT hardcode lens names server-side, so future taxonomy tweaks are frontend-only. Decision #56's data-defined-not-enum-defined rule preserved | Active |
| 69 | 2026-04-30 | Status taxonomy in [server/services/daily-sales/parse.js](server/services/daily-sales/parse.js) as four mutually-exclusive named Sets + `statusBucket(rawStatus)` helper. Buckets: **Realized** = {Delivered, Fullfilled (+typo), Partially Fullfilled (+typo)}, **In Flight** = {Order Placed, Processing}, **Cancelled** = {Cancelled, RTO, Returned, Lost}, **Draft** = {Draft Order}, **Other** = unknown raw values. Every parsed Order carries `o.statusBucket` so downstream code never re-classifies. All Sets case-insensitive (lowercase entries) | The implicit `REALIZED_STATUSES` Set was the only named bucket — extracting In Flight / Cancelled / Draft as siblings makes the mental model explicit and gives the lens math a single source of truth. Without this, "Net of cancellations" and "Active pipeline" definitions would have been duplicated across backend and frontend, drifting over time. Mutual exclusivity verified by code-reviewer (no status appears in two buckets). "Lost" placed in Cancelled per current FY data semantics. Adding `o.statusBucket` at parse time keeps the cost O(1) per order; `buildMix` and tooltip components read the bucket directly | Active |
| 70 | 2026-04-30 | Print method + status casing collisions in `buildAvailableFilters` ([server/services/daily-sales/index.js](server/services/daily-sales/index.js)) fixed at READ side only — Map keys lowercased, first-seen display casing preserved. Source sheet is NEVER mutated. Same pattern as `buildMixByFn` in [aggregations.js](server/services/daily-sales/aggregations.js). DTG/dtg/Dtg collapse to one filter chip; "Delivered"/"delivered" same | Source data has organic case variants (free-text user input into the sheet) and the variants will keep accumulating. Mutating the sheet is destructive (loses raw audit trail), creates a sync problem (next import overwrites the cleanup), and violates Decision #56's data-defined-not-enum-defined principle. Mutating on read is idempotent and cheap. First-seen-casing-wins is the simplest display rule and matches the existing `buildMixByFn` precedent — code-reviewer confirmed no behavior drift | Active |
| 71 | 2026-04-30 | Blank-`Order #` rows in the Daily Sales sheet bypass the dedup Map (treated as unique-by-row, not collapsed to a single key). Was a real bug: 4 Draft Order rows (₹72,408 total — Nirmal Dayaramani, DRIPACCO, Gautham Siju, Manoj) all collided on key `""` and 3 of 4 were silently dropped. After fix, current FY reports `dataQuality.total: 1201` (was 1198) and `mix.status` shows `Draft Order: 4` (was 1) | Drafts don't have an Order # because they pre-date order assignment; collapsing them by Order # treats different orders as the same order. Doesn't affect realized revenue (drafts aren't realized) but corrupted gross counts and `dataQuality.excludedByStatus`. Fix applied to BOTH `deduplicateIntraTab` and `deduplicateCrossTab` to keep the bug from re-surfacing in cross-tab reconciliation. Original "last write wins for non-blank Order#" semantics preserved (matters for cross-tab where month-tab should win over YTD-tab on overlap) | Active |
| 72 | 2026-04-30 | `server/services/daily-sales/` split from a 637-line `index.js` god-file into 5 sibling files: `index.js` (75-line public-API shim), `cache.js` (cache mgmt + LRU+TTL), `fetch.js` (Sheets fetch + dedup), `available-filters.js` (dropdown options + casing fix), `dashboard-builder.js` (payload composition orchestrator). Pre-existing siblings unchanged: `parse.js`, `filters.js`, `aggregations.js`, `data-quality.js`, `fy.js`. Public API frozen — every previously-exported symbol still resolves identically from `require('./services/daily-sales')`. Pure mechanical refactor, zero behavior change, 139 tests pass unchanged | The 637-line `index.js` was the biggest agent-orientation tax in the package: every backend round (Phase 2 backend, casing fix, dedup fix, etc.) had agents reading the whole file 2-3× to find the right block. Each split file is now <100 lines except `dashboard-builder.js` (the irreducible orchestration). Future agent runs read 75-400 line files instead of 600+. Pattern is now a template for other god-files — `server/services/notion.js` (~1000 lines) is next; `src/js/app.js` after that. Dep graph stays acyclic: parse + fy are leaves; cache → filters (for DEFAULT_FILTER_SPEC reference equality); fetch → parse + fy + sheets (lazy); dashboard-builder → all of the above; index.js → the orchestrator + cache | Active |
| 73 | 2026-04-30 | Lazy `require('../sheets')` inside function bodies (in `fetch.js` and `dashboard-builder.js`) is the contract for any sibling module in a package that consumes a mocked external service. Module-top `const { fetchSheet } = require('../sheets')` is BANNED in these files | Tests inject mocks via `require.cache[SHEETS_PATH] = { exports: { fetchSheet: stub } }` and clear `index.js` from cache between describe blocks. Sibling modules stay cached — module-top requires capture stale closures and bypass the mock. Lazy require honours the live cache state at invocation time. Cost: per-call Node require-cache lookup, negligible. Applies symmetrically to any future external service (notion, github, db) that gets mocked the same way. Documented in [`server/services/daily-sales/FILE-MAP.md`](server/services/daily-sales/FILE-MAP.md) so the constraint survives future splits | Active |
| 74 | 2026-04-30 | Per-package `FILE-MAP.md` becomes the contract for any service folder with >2 files. Format: (a) public exports table with consumers, (b) per-file "owns" + "DO NOT add" sections, (c) "Want to add a feature?" routing table mapping intents to files, (d) explicit dep graph, (e) references to constraining decisions. First instance: [`server/services/daily-sales/FILE-MAP.md`](server/services/daily-sales/FILE-MAP.md). Subsequent god-file splits (notion.js, app.js, dashboard.html) MUST ship a FILE-MAP alongside | Reduces agent orientation cost from 60-90s of grepping ("where is X defined? what calls Y?") to a 10-second read. Splitting alone doesn't help if agents still have to discover the topology each session. The FILE-MAP encodes the topology in one place that's small enough to fit a Read tool call. Decision references inside the FILE-MAP keep semantic constraints visible at the package level (vs. buried in 70+ rows of decisions.md). Maintenance cost: scribe updates FILE-MAP whenever the routing table changes — same lifecycle as `app-reference.md` | Active |
| 75 | 2026-04-30 | All agents follow the methodology codified in [`.claude/rules/file-discipline.md`](.claude/rules/file-discipline.md) when reading/editing/refactoring code. Auto-loaded for all paths (`paths: "**"`). Covers: (a) **pre-flight order** before editing — CLAUDE.md → per-package FILE-MAP.md → app-reference.md grep → decisions.md grep → target file with offset/limit; (b) **file-size ceilings** — 250/400 soft for service handlers, 400/600 for orchestrators, 300/500 for frontend; hitting hard cap forces a split before any new lines land; (c) **split methodology** — public-API shim + per-concern siblings + acyclic dep graph + FILE-MAP.md alongside; (d) **lazy-require contract** — `require('../sheets')` inside function bodies for any sibling module consuming a mocked external service; (e) **refactor contract** — public API frozen, behavior frozen, tests untouched, dep graph acyclic, lint clean. Cross-referenced from `workflow.md` step 6 | Codifies what we proved in the daily-sales restructure (decisions #72–74) into a rule all agents auto-load. Without this, the methodology lives only in this session's transcript and fades within a few sessions. The rules folder is the right place — it complements `token-efficiency.md` (HOW to read cheaply) and `workflow.md` (WHEN to dispatch) with the missing layer: HOW to make changes safely. The "when to break these rules" section at the bottom is deliberate — emergency hotfixes and single-line fixes shouldn't pay the full pre-flight cost; the rule exists to prevent god-file regression, not to bureaucratize trivial edits | Active |
| 78 | 2026-05-01 | App split spec [`docs/app-split-config-spec.md`](docs/app-split-config-spec.md) shelved for now. Will revisit when `server/services/notion.js` god-file split lands (next backend target, ~1000 lines) or `src/js/app.js` crosses 2500 lines, or a real bug forces touching the 3-map LAZY_MODULE_FACTORIES + _viewStyleFile + _partialViewClass pattern simultaneously | (a) spec is half-baked (factual errors, typos, 5 concerns bundled — see #77); (b) higher-leverage split work already queued — notion.js + frontend daily-sales mirror-split prove the pattern at smaller surface area; (c) Daily Sales reconfigure is the active priority — touching app.js mid-flight risks cross-contamination; (d) ROI is dev ergonomics not user value, pain isn't acute (1879 lines is annoying not blocking) | Shelved |
| 77 | 2026-05-01 | App split spec at [`docs/app-split-config-spec.md`](docs/app-split-config-spec.md) (OpenCode CLI output, marked DRAFT) needs three changes before any work begins: (a) factual cleanup — module count is 29/9,988 lines, not 39/11,867; partial count is 27, not 26; cosmetic typos throughout (`shell. js`, `meta. json`, etc.); (b) split into 3 separate proposals — structural split + registry, URL routing, declarative `fetchOnEnter` — each gets its own decision row; (c) drop `meta.json` (registry is single source); drop TypeScript `.d.ts` and e2e tests from the migration plan (independent calls). No code shipped — review only | Per decision #75 (file-discipline), mechanical splits don't bundle behavior changes. The spec as written bundles 5+ concerns under one "split" label, which violates the refactor contract (public API + behavior frozen during a structural move). Treating it as a single migration plan masks the real scope and underestimates effort (Phase 1 listed as "1 day" — moving 27 views with co-located module/html/css plus shipping at end of day is closer to 2-3 days). Each behavior change needs its own decision + reviewer pass | Pending Dan |
| 76 | 2026-05-01 | Verified the three failing tests in isolation before applying any fixes. Real findings differ from the user-supplied diagnosis on all three: (a) `dan-colin-service.test.js` has 4 `afterEach is not defined` failures, not 1 — fix is `const { describe, it, before, after, afterEach } = require('node:test');`, NOT renaming `afterEach → after` (changes semantics); (b) `auth-gate.test.js` does NOT fail with "Promise resolution still pending" — all 6 assertions pass; the test process *hangs* until `--test-timeout` because [server/middleware/auth-gate.js:160](server/middleware/auth-gate.js#L160) calls `setInterval(...)` without `.unref()`, pinning the event loop. Real fix is in production code, not the test; (c) `notebooks.test.js:171` reference-equality failure is real, but the proposed `deepStrictEqual` fix silently breaks the test's stated intent (cache-hit verification). Correct options: cache the missing-file response, or set up a real registry file in `before()`. No code changes shipped — pending Dan's go-ahead | Diagnose, don't pattern-match. A user-supplied "fix" can be wrong in three ways: wrong root cause (auth-gate), wrong scope of failure (dan-colin had 4 failures, not 1), or right symptom + wrong fix that silently breaks intent (notebooks `deepStrictEqual` would pass even with caching disabled). Always run the failing file in isolation against `node --test` before trusting third-party diagnosis. The pattern of `--test-timeout` hangs masquerading as "Promise resolution pending" is worth remembering — `setInterval`/`setTimeout` without `.unref()` in any module a test requires will pin the event loop forever | Active |
| 80 | 2026-05-01 | `server/services/sheets.js` god-file split — flat package under `server/services/sheets/` with 9 single-concern files + FILE-MAP.md + 19-line eviction-loop shim at original path. Public API (16 exports: `getPipelineData, getStrategyCascade, isConfigured, clearCache, SHEET_REGISTRY, SPREADSHEET_KEYS, isSpreadsheetConfigured, getSpreadsheetId, fetchSheet, appendRow, updateRow, deleteRow, getSheetHeaders, parseRows, resolveSheetName, getSheetLink`) frozen. **Single PR** (vs 3-PR phasing on notion) — 759-line file with simple acyclic graph is reviewable in one extraction. Strategy-cascade preserves its inline GoogleAuth client (uses `STRATEGY_SHEETS_ID`, not `GOOGLE_SHEETS_ID`) — behavior frozen, no consolidation with `getReadWriteClient()`. Lint clean, 1014/1014 tests pass on two consecutive runs | Same mechanical-extraction methodology as daily-sales (#72) and notion (#79). Largest sibling 195 lines (crud.js, registry.js) — well under 250 soft cap. Dep graph: 5 leaves (cache, parse-rows, resolve-sheet-name, client, registry) → 2 mid-layer (tab-meta, crud) → 2 domain composers (pipeline, strategy-cascade) → index re-export. No file requires `./index`; no cycles | Active |
| 81 | 2026-05-01 | **Eviction-loop shim pattern** for any package whose tests inject mocks via `require.cache[ENTRY_PATH]` and rely on `delete require.cache[ENTRY_PATH] + re-require → fresh module state`. Required for sheets (NOT needed for notion per #79). The shim deletes every `require.cache` entry under the package directory before loading `./pkg/index`, restoring the pre-split semantics where the entire 759-line module re-ran on each cache eviction (recreating Map state + resetting `let` client singletons to null). Production impact zero — shim cached after first server-startup load; eviction loop runs once against an empty cache. **Why a simple `pkg.clearCache()` in the shim is INSUFFICIENT:** clearCache only resets the main `cache` Map, NOT `tabMetaCache` (separate Map in tab-meta.js) and NOT `let sheetsClient`/`let rwClient` singletons in client.js. Tests like `"hits the Sheets API only once"` failed `0 !== 1` because tab-meta from a prior test polluted state across tests. The eviction loop is the only fix that mimics the original module-reload contract for ALL state, not just public Maps | Distinct from #79's "lazy-require NOT needed" finding for notion: notion tests don't mock at the require.cache level, but sheets tests DO (`test/sheets-service.test.js` injects fake config + googleapis at `require.cache[CONFIG_PATH]`/`require.cache[GOOGLEAPIS_PATH]` then deletes `require.cache[SHEETS_PATH]` to force a fresh load). Apply this pattern symmetrically to any future package whose entry path appears in `require.cache[X]` mock injections | Active |
| 79 | 2026-05-01 | `server/services/notion.js` god-file split methodology — flat package under `server/services/notion/` with 24 single-concern files + 3-line shim at current path (preserves `require('../services/notion')` for all 38+ callers). 3-PR phasing: PR1 = infra leaves (9 files), PR2 = domain reads + writes, PR3 = composers + entry shim + FILE-MAP.md. Public API (56 exports) frozen across all 3 PRs. Each PR independently revertible. Lazy-require NOT needed (verified: tests do not inject `getClient()` mocks via `require.cache`, unlike daily-sales/sheets pattern) | Same methodology as daily-sales (Decisions #72–75) — proven to keep tests green during mechanical refactor. The shim pattern IS the "duplicate + re-point" backup Dan asked about: if any PR breaks, single `git revert` puts the old monolith back. Avoids literal duplicate files (which create divergence risk). 3-PR phasing keeps each commit small enough to review (each PR is ~700 lines moving) | Active |
| 82 | 2026-05-01 | Phase 3 CSS safety net — load `src/css/styles.css` (the legacy monolith) alongside `core.css` in `public/index.html`. Build script extended to emit `public/css/styles.css` (~218KB minified). Restores every shared class still trapped in styles.css after the incomplete Phase 3 split. Temporary: removed once a lint script identifies all orphaned classes and a migration pass moves them into `core.css` (shared) or `views/<view>.css` (single-view) | Phase 3 was supposed to retire styles.css by migrating every used class into the new structure. Migration was mechanical-incomplete — at least 4 class families confirmed broken across views (`mktops-area-metric-*` on 5 views, `brief-banner-*` on dashboard, plus owner-workload action buttons on CRM, plus the round-1 `mktops-overview-shell/hero/priority-card` set already fixed). Whack-a-mole each family is wasted churn — load styles.css as a safety net, then build a checker to size the real backlog before committing to a fix scope. styles.css already contains every rule; loading it after core.css is redundant but safe (same rules cascade). Cost: 218KB extra CSS until retired | Active |
| 85 | 2026-05-02 | **Pre-write methodology for batched CSS cleanup** — when a multi-step CSS triage hits agent stream-cap during investigation, the orchestrator pre-investigates from its own context (cheap greps) and dispatches with a concrete action list — full CSS rules inline, file destinations specified, no investigation in-prompt. Each batch becomes "apply these N Edit calls + run gates" rather than "investigate then fix." Phase 3 Leg 3 used this to drain 69 of 81 leakage rows across 4 batches with zero stream-cap failures, after two prior in-investigation attempts had both capped mid-task. Pattern: orchestrator does all greps + markup inspection + cap-watching + style-pattern matching → dispatches narrow agent with full diff template → agent's only job is mechanical Edit + validation gates | Stream-cap is a real harness limit (~60 tool calls in our env). Investigation tool calls (grep, file Reads to find neighbors, markup pattern checks) consume that budget faster than mechanical Edit calls. Moving investigation to the orchestrator (which retains its own budget across batch dispatches) halves per-batch tool count and keeps each agent dispatch well under cap. Reusable for any future leg-style cleanup that touches >5 files | Active |
| 86 | 2026-05-02 | **Empty marker CSS rules** are the canonical fix for wrapper classes that exist purely as semantic-grouping or JS-hook targets and have NO standalone styling (their visual effect comes from a sibling `.foo` base class, suffixed children `.foo-content`, or an inherited `.bar` from compound markup). Phase 3 Leg 3 added them for `.brief-banner-overdue/-top3/-flags/-waiting` (siblings to `.brief-banner-section`), `.ds-geo-row` (modifier `--drillable` does the work), `.bmc-block--static` (variant marker), `.attention-upcoming` (4th attention-count modifier), `.context-menu-trigger` (JS hook for `:not()` selector). Each gets a comment block explaining intent | Three alternatives are worse: (1) artificial no-op styling = lying about intent, (2) deleting the bare class from markup = breaks JS hooks + AT semantics, (3) upgrading linter to special-case "compound prefix exists" = false-positive risk + fragile regex. Empty rules are honest, lint-clean, regex-friendly to any future linter, and document the design choice. Pattern: comment block above the rule(s) explaining why it's empty + where the styling comes from | Active |
| 84 | 2026-05-01 | `scripts/check-css-leakage.mjs` — 3-tier Alpine state whitelist mechanism. `KNOWN_ALPINE_STATE_PATTERNS` (regex array — `/Loading$/`, `/^systemSyncing$/`, literal `view-loading`), `KNOWN_ALPINE_STATE_CLASSES` (Set of camelCase global state literals — dropSubmitSuccess, dcAnswerFocused, watchExpanded, closedExpanded, factoryShowFormulas, factorySimOpen + status enums Accepted/Awaiting/Rejected), `KNOWN_ALPINE_STATE_PER_PARTIAL` (Map keyed by partial — daily-sales × {realized, status, all, acceptance, date, state}). Plus orphan-row counter at report header (closes the audit-miscount root cause from 22:25). Net effect: leakage report stops false-positive-flagging Alpine reactive variable names that the script's class-token regex picks up from `:class="state && 'foo'"` bindings | The linter previously couldn't distinguish CSS class references from Alpine state names. Whitelisting at 3 tiers is necessary because: tier 1 (regex) handles shape-based families like all `*Loading` flags without enumerating each; tier 2 (literal Set) handles one-off camelCase names that have no reusable shape; tier 3 (per-partial Map) handles generic words like `status`/`date`/`all` that are unambiguously Alpine state inside daily-sales but could be real CSS classes elsewhere — blanket-whitelisting them globally would weaken leakage detection. Extension protocol documented in script header. Dropped 19 false positives from the leakage report (101→82) without weakening detection of real cross-leaks | Active |
| 83 | 2026-05-01 | Dashboard area-metric strip redesign — Option A (tone-coded cards) chosen over Option B (cards + sparklines) and Option C (bento spotlight layout). `getDashboardHeroMetrics()` extended with `tone: 'critical'|'warning'|'healthy'|'neutral'` field per metric, computed server-side from existing in-scope vars (no new fetches). CSS adds 4 tone modifiers to `.mktops-area-metric-card` (base moved from marketingOps.css to core.css) — border + label + value color + subtle radial gradient bg + critical/warning notes render as colored pills. Icon slot top-left (lucide via `data-lucide`), chevron top-right revealed on hover, 2px box-shadow lift. Mirrors the `mktops-hero--{tone}` pattern already in core.css. Spec at `design-system/specs/dashboard-area-metric-toned.md`. Hover rule deviation: builder swapped `color-mix(currentColor, …)` for box-shadow + translateY because `currentColor` at the card level is text color, not the tone accent — would have produced incorrect hover borders. Tone-matched borders deferred as a per-tone hover rule follow-up | Smallest mechanical change with highest signal/effort ratio. Sparklines (B) need backend trend arrays + hand-rolled SVG (per #67); bento (C) needs design-planner pass on visual identity for all 7 area-dashboards. Both deferred until A validates the direction visually. The data was wasted in the flat design (50/54 commitments overdue rendered as muted grey) — tone-coding alone closes the gap. Backend is client-side `dashboard.js` not server (function lives in the frontend module — backend-builder verified the heroMetrics array in `ceo-dashboard-service.js:864` uses different IDs and doesn't drive the strip). Reuses existing red/amber/green tokens, no new hex | Active |
| 88 | 2026-05-02 | **Bypass per-view file-discipline split for Phase 3 Batch 5** — routed final 12 leakage-flagged classes through `src/css/core.css` (already 11K-line god-file, +87 lines is noise) instead of waiting on 5 separate view-CSS splits (google-ads.css 672, dashboard.css 676, daily-sales.css 2836, marketingOps.css 565, techTeam.css 414). Block contains 6 wrapper markers (compound class — base does the work) and 6 minimal real rules (error/unavailable banners, tab-body padding, count badge). Tagged with comment block stating "Once view-CSS splits land (queued open-loop), these can migrate" | The file-discipline hard-cap rule exists to prevent god-files from accreting unbounded — but core.css is already a god-file by any measure (11K lines), and 5 view-CSS splits to land 30 lines of mostly-marker rules is wildly disproportionate. The view-CSS splits are still queued as regular tech-debt (not blocking anything anymore), so when they happen the markers can migrate to their proper homes. Net effect: leakage 12→0 in one batch, file-discipline backlog unchanged. The exception is principled (size disparity, marker-heavy mix) rather than ad-hoc; future leg-style cleanups should ask "is this a marker family or real styling?" before deciding bypass-vs-split | Active |
| 87 | 2026-05-02 | `server/services/google-ads-service.js` — drop `Data_Crunching` source tab entirely and rebuild campaign rollup + daily trend from `Raw_Data`. `parseCrunchingRows` deleted (~82 lines); `parseRawRows` extended to ~97 lines, now returns `{ campaignList, dailyTrend, dates, byDevice }`. `getDashboard` fetches 2 sheets instead of 3. Conversions are now fractional floats (real Google Ads attribution: e.g. `0.476`, `8.887`, summed directly) instead of binary `hasConversion ? 1 : 0` (which counted days-with-conversions, not actual conversions — old behavior was a bug). Public API frozen for frontend; one renamed `meta` field (`crunchingRows` → `rawRows`) is non-breaking. Live verification: spend ₹1.37 quadrillion → ₹6.3L; clicks 0 → 310K; conversions 0 → 14,441; revenue ₹0 → ₹31L. 1014/1014 tests, lint clean, code-reviewer APPROVE | `Data_Crunching` tab is structurally broken at the source: 12 cols but no clicks/cost/conversions/revenue cols exist anywhere; col 6 "Real Cost" contains ad_group_id with a ₹ formatter; col 8 "Revenue" is `#VALUE!`. Earlier in this session the `Raw_Data` tab header row was rewritten to 23 correct labels via service-account RW client (path-a fix), so it now contains every metric needed in clean form. Header rewrite for `Data_Crunching` would relabel cols accurately but the data the parser needs simply isn't there — fixing headers can't recover columns that don't exist. Cleaner to drop the dependency entirely and source from `Raw_Data` (now canonical). Conversions-as-floats is intentional: Google Ads reports fractional attribution (one click can attribute 0.5 of a conversion across overlapping campaigns); summing the binary "had any conversion" flag was a 2-3 order-of-magnitude undercount. Frontend formats; no template change | Active |
| 89 | 2026-05-02 | JSON 404 handler scoped to `/api/*` mounted before the SPA catch-all in [server.js:177-183](server.js#L177-L183). Any unknown `/api/*` path now returns `404 application/json` `{"error":"Unknown API endpoint: GET /path"}` instead of falling through to `app.get('*')` which serves `public/index.html` as `200 text/html`. Verified locally: `curl /api/dashboard` → 404 JSON, real `/api/health` → 200 JSON. Lint clean, 1014/1014 tests | Missing API endpoints masquerading as 200 HTML responses break smoke tests, fool clients that branch on `Content-Type`/`status`, and silently swallow typos in route names. The catch-all at line 177 is correct for SPA routing (any HTML route should serve the shell), but it should NOT serve the shell for `/api/*` — those are pure JSON contracts. Scoping the 404 to `/api` keeps the SPA fallback intact for HTML routes while making the API surface honest. Same diagnosis-pattern as the `/api/health/details` 500 fix (`c1aee8b` earlier this session): a route layer was returning the wrong shape because of a missing guard | Active |
| 90 | 2026-05-02 | **Apply file-discipline split methodology to `src/css/views/daily-sales.css`** (was 2836 lines, biggest god-CSS in repo, 4.7× over the 600-line hard cap). Split into 1 entry-point shim (18 lines, `@import` only — preserves `/css/views/daily-sales.css` public API used by frontend lazy-loader at `src/js/app.js:149`) + 10 leaves at `src/css/views/daily-sales/{base,filter-strip,lens,mtd-channel,trends,mix,top-states,todays-orders,concerns,data-quality}.css` (largest leaf 493 lines, all under hard cap) + FILE-MAP.md. esbuild's `bundle: true` (`scripts/build-assets.mjs:46`) inlines the @imports natively at build time → single bundled output unchanged in shape. `readdir` glob filters by `.css` suffix so the subfolder is invisible to the build entry-point list. Behavior frozen, public API frozen, tests untouched. 1014/1014 tests, lint clean, build clean. Bundle size 42089→40367 bytes (minor minification efficiency gain on cleaner cluster boundaries) | The hard-cap rule says "the next change MUST be a split — no exceptions." daily-sales.css was already 4.7× over and queued in open-loops since 2026-05-02 from Phase 3 cleanup. Inline-style migration of `daily-sales.html` (22 instances, next punch-list item) was blocked behind it. Splitting first unblocks: (a) the inline-style migration, (b) any future view-CSS work on this surface, (c) safe additions of the 1 deferred Phase 3 marker (`.ds-weekly-chart`) without further bypass. Cluster axes follow the 9 visible panels in `public/partials/daily-sales.html` (filter strip, lens, MTD/channel, trends, mix, top states, today's orders, concerns, DQ) + 1 base/shell leaf for view chrome — matching how rules are added in practice (per-panel intent). Two inevitable cross-family exceptions documented in FILE-MAP.md: `.ds-status-*` color-token badges live in base.css alongside `.ds-status` page-level chrome (shared prefix); compound rule `.ds-channel-pill, .ds-tag-method { ... }` kept intact in mtd-channel.css to honor no-rule-edits contract. Same methodology as Decisions #72 (notion split) / #79 (sheets split) / #80 (daily-sales backend split). After this, the only remaining unsplit view-CSS over hard cap is `views/marketingOps.css` at 600 (exactly at cap) | Active |
| 91 | 2026-05-02 | Use separate `Pillar (IG)` SELECT on Content Calendar (5 IG pillars) instead of expanding existing `Content Pillar` enum (6 cross-channel options). Both fields coexist on CC; non-IG content leaves `Pillar (IG)` null | The 5 IG pillars from playbook §2 (Permission/Napkin/In-the-Wild/Craft/Educational) are a different taxonomy than the existing 6 cross-channel pillars (Education/Social Proof/Product/BTS/Community/Promotional). Merging would create an 11-option enum with overlapping semantics ("Education" vs "Educational") that breaks IG measurement slicing. Reversal cost: low (drop the column). The IG Performance rollup `Pillar` reads from `Pillar (IG)` specifically — co-locating both axes lets cross-channel analysis stay clean while IG slicing stays IG-only | Active |
| 92 | 2026-05-02 | `IG Performance.Hit Target` formula inlines the SWPS calculation (`if(prop("Reach") == 0, false, (prop("Saves") + 2 * prop("Shares")) / prop("Reach") >= 0.035)`) instead of referencing the SWPS formula property via `prop("SWPS") >= 0.035` | Notion Formula 2.0 errors with `Type error with formula` when one formula property references another formula property in a `>=` comparison. Empirically verified during DDL creation — both `prop("SWPS") >= 0.035` and the canonical IG Playbook §3.4 spec failed with that exact error. Inlining duplicates the SWPS math but works. Maintenance cost: if SWPS formula is ever renamed or its math changes, Hit Target's inlined copy must be updated in lockstep. Documented in `docs/marketing/NOTION-SETUP.md` § 5.1 | Active |
| 93 | 2026-05-02 | `IG Performance.Week Of` formula uses flat `formatDate(prop("Published Date"), "YYYY-MM-DD")` instead of the IG Playbook §3.4 spec `formatDate(..., "YYYY-WW")` for ISO week | Notion's `formatDate` does not accept `WW` (ISO week) tokens, and Formula 2.0 has no `week()` date function (only `month()`/`year()`/`day()`). For ISO-week grouping, use Notion's built-in "Group by Week" view filter on `Published Date` directly — the formula property is decorative until/unless Notion ships ISO-week support. Documented in `docs/marketing/NOTION-SETUP.md` § 5.2 | Active |
| 94 | 2026-05-02 | Augment `docs/marketing/`, don't replace: the marketing-system index lives at top-level `NOTION-SETUP.md`; the existing MCC-Posts-only doc was renamed `mcc/POSTS-DB.md` and other MCC docs (ARCHITECTURE/API/SETUP/USER_GUIDE) moved to `mcc/` subfolder. README.md updated to index the new structure | The original `NOTION-SETUP.md` was wired to live MCC code (env var `MCC_POSTS_DB_ID` reads the file path is documented in there, and `read-model.js` line links go to it). Replacing it would have created stale links + risked confusion about which doc is authoritative for the MCC code path. Augmenting keeps MCC self-contained while exposing the broader marketing-system map at the top of the folder. Sets up the pattern: future channels (`ads/`, `lifecycle/`) get sibling subfolders, `NOTION-SETUP.md` stays as the index. 6 relative paths in `mcc/POSTS-DB.md` updated from `../../` to `../../../` to compensate for the deeper nesting | Active |
| 95 | 2026-05-02 | Phase the marketing-system wire-up A→B→C→D (foundation→backend reads→frontend UI→automations) instead of a single big-bang | Reduces blast radius if any phase blocks; A is risk-free read-only setup, B is read-only API (no approval gate), C surfaces UI on real data, D adds writes (highest risk, last). Dan can interrupt or redirect after each phase. Matches NOTION-SETUP.md §6's own P0/P1/P2 priorities | Active |
| 96 | 2026-05-02 | Create `server/routes/marketing-ig.js` as a new file (do NOT extend `marketing-ops.js`) | marketing-ops.js was already 312 lines — over the 250 soft cap. Adding 9 endpoints would have pushed it past 400 hard cap and forced a same-PR split. New file keeps the IG read surface logically distinct (read-only, no approval gate, no SSE) and preserves marketing-ops.js for the existing campaign/content/sequence/task surface. Honors file-discipline.md "when you hit the soft cap, propose a split before adding lines" | Active |
| 97 | 2026-05-02 | Make Content Calendar IG validators additive — keep `VALID_CONTENT_PILLARS` (Education/Social Proof/Product/etc.) alongside new `VALID_IG_PILLARS` (Permission/Napkin/etc.); ditto for `VALID_HOOK_PATTERNS` and `VALID_PUBLISHED_SLOTS` | Per Decision #91, the IG and cross-channel pillar taxonomies are different axes. POST/PATCH `/content` must accept either or both. Removing the old enum would break existing CC writers (other modules + future MCC integration) for zero benefit. Old callers omitting the 3 new fields stay unaffected. Pattern: `igPillar`/`hookPattern`/`publishedSlot` are optional body params validated only-if-present | Active |
| 98 | 2026-05-02 | Env-var-gate the 5 IG playbook DB IDs at request time (`process.env.X \|\| null` in `notion/databases.js` → `requireDbId()` throws clear error in readers) instead of fail-fast at app startup | App must boot even without IG env vars set (they're new + optional until Phase C UI lands). Throw-at-request-time gives Dan a specific pointer to docs/marketing/NOTION-SETUP.md §7 when an endpoint is hit without env populated, instead of silent empty results that look like a Notion outage. Other readers (notion, sheets) follow the same pattern | Active |
| 99 | 2026-05-02 | Build IG Playbook as a new `/ig` view (sidebar entry under Growth) instead of embedding sections into `/marketingOps` | All 3 marketingOps surface files were over file-discipline hard caps (HTML 1761/500 — 3.5× over, JS 1085/500 — 2.2× over, CSS 600/600 — exactly at). Embedding would have forced splitting all 3 before any IG code lands (~6+ hrs of pure mechanical refactor with nothing visible to Dan). New `/ig` view ships in ~3 hrs, mirrors the existing `/mcc` separation pattern, keeps IG measurement (north-star metric) logically distinct from campaign/sequence management. The marketingOps refactor becomes its own ticket whenever Dan next needs to touch that view, decoupled from feature delivery | Active |
| 100 | 2026-05-02 | **SUPERSEDES #98** — Hardcode the 5 IG-playbook DB UUIDs in `server/services/notion/databases.js` (matching the existing 18 DBs pattern) instead of env-var-gating them | The env-gating in #98 created a real-world blocker: Dan would have had to add 5 vars to `.env` + Vercel + restart before `/ig` view could populate. The IDs are stable forever, already public (notion-hub.md, NOTION-SETUP.md, GitHub repo), and the workspace is single-tenant. Single source of truth in `databases.js` matches the existing pattern (Commitments/Decisions/Projects/etc. all hardcoded). Eliminates env-setup friction; the only requirement is `NOTION_TOKEN` having access to "YDS Marketing Hub" page. Deleted 1 obsolete env-var-guard test (test/marketing-ig.test.js); listDatabases() extended to expose all 5 new DBs (1018 → 1023 entries). `requireDbId()` defensive check in reads/marketing-ig.js kept as no-op (always-truthy now); will be cleaned up during Phase D per-DB split | Active |
| 101 | 2026-05-02 | Hub B `Campaigns` (cff40f34) is the canonical campaign DB. Harvest 4 fields from `Marketing Campaigns` (16b0b0): `Type`, `Spent`, `Target ROAS`, `Actual ROAS`; rename 16b0b0 with "(old)" suffix instead of archiving | Three campaign DBs exist (Hub B Campaigns, Hub B Marketing Campaigns 16b0b0, Hub A YD Marketing Campaigns). Hub B Campaigns wins because already wired and has unique fields the app uses: `Stage` workflow (Briefing→Build→Live→Optimize), `Owner` select tied to YDS team names, `Decisions`+`Focus Area`+`Project` relations. 16b0b0 contributes paid-media tracking (ROAS/Spent/Type) which Campaigns lacks. Hub A's `YD Marketing Campaigns` rejected — over-engineered (4-bucket budgets, Customer Personas, Risk Factors+Mitigation, Lessons Learned, Weekly Updates) for solo-CEO ops. Per Dan's directive don't archive — rename retired DBs with "(old)" suffix to keep history visible without UI confusion | Active |
| 102 | 2026-05-02 | Hub B `Content Calendar` (a3066b81) is the canonical content DB AND the MCC posts DB (no separate MCC_Posts DB). Harvest 6 fields from Hub A YD Content calendar: `Content Series`, `Repurpose Opportunities`, `Seasonal Tag`, `CTA`, `Tracking URL`, `Hashtags`. Add 2 MCC-specific fields: `Media URLs`, `Platform Post IDs` | Hub B Content Calendar already structurally superior for execution: IG publishing slots, Hook Pattern relation, Brand Review Status, IG Performance link, Template relation, 8-state Status pipeline (`Idea→Briefed→Drafted→In Design→Brand Review→Approved→Scheduled→Published`). Hub A adds India-critical context (Diwali/Wedding Season seasonality), themed-day series, CTA per post, tracking URLs, hashtags. Skipped Hub A's Production Cost/Revenue Attribution/Hours/Priority/Next Actions/Performance Metrics — duplicates Status, replicates page body, premature ROI tracking. Status pipeline maps 1:1 to MCC lifecycle — single calendar truth eliminates broken `MCC_POSTS_DB_ID` placeholder | Active |
| 103 | 2026-05-02 | Hub A "Marketing" page (25c247aa) DBs to be **renamed with "(A)" or "(old)" suffix**, NOT archived. Do not wire any Hub A DBs to the app | Hub B (YDS Marketing Hub) is single source of truth per #101+#102. Per Dan's directive 16:30, retired DBs stay visible in Notion (not archived) so historical data + structures remain inspectable; "(old)" suffix prevents accidental writes during transition. Specialty Hub A DBs (Sprints, Funnel, Audience Segments) deferred — if needed later, re-create simplified versions under Hub B parent rather than wire Hub A's structures. Row-count audit deferred since archive isn't happening | Active |
| 107 | 2026-05-03 | **Marketing Tasks DB merged into Commitments.** All 6 active rows (5 Not Started + 1 In Progress, all overdue from March 2026) migrated to Commitments with Focus Area = D2C Marketing & Content (`307247aa0d7b81f98b98f77809174063`), Source = Claude Session, original due dates preserved. Provenance recorded in each Commitment's Notes field via `[Migrated from Marketing Tasks 2026-05-03 — orig TSK-N]` tag plus the deprecated old-Campaign URL. New Commitment IDs YDS-262 through YDS-267. Source DB renamed to `[ARCHIVED] Marketing Tasks (migrated to Commitments 2026-05-03)` with description explaining the migration; Notion auto-DB-rename serves as soft lock. EA-BRIEFING.md § 4 + § 6 updated to point engineers at Commitments (filter by Focus Area) instead of Marketing Tasks | Single source of truth for tasks across the company. Marketing Tasks was a tech-side artifact never listed in marketing-team's canonical 9 DBs (per `YDS - marketing-team/CLAUDE.md`). Schema fit was clean: Commitments already has Focus Area, Audience, Platform, Project, Priority, Type, Source, Review Cadence — every property Marketing Tasks needed. Channel select preserved as text in Notes since Commitments doesn't have a Channel field (Platform relation is the analog but mapping requires a separate Platforms DB lookup). Old Campaign relation (pointed to deprecated `2e213a71-…` Marketing Campaigns DB, not canonical `cff40f34-…`) was dropped — preserved as URL in Notes for traceability. People relation skipped (Marketing Tasks Assigned To used Notion person field, Commitments uses People-DB relation; no clean auto-map). Dan can re-assign the 6 commitments manually in next sweep. Closes the EA-briefing alignment loop (decisions #105 + #106) | Active |
| 109 | 2026-05-03 | **YDS Marketing App spec saved as `docs/MARKETING-APP-SPEC.md`** — authoritative build target for marketing surface (8 pages × 16 DBs × CRUD contract). Cross-referenced to NOTION-SETUP.md (DB IDs/schemas) + TECH-WIRE-UP.md (wiring punch list) + MARKETING-ALIGNMENT.md (Phase 1/2 split). Includes per-page DB cheat sheet with all 14 marketing data-source IDs + dependency-ordered build sequence + current shipped state per page | Single source of truth for agents and Wire/Nirmal so no one rebuilds from scratch. Canvas was Dan-only; persisted doc means future agent dispatches can grep it via `app-reference.md`-style pre-flight. Sequencing chosen to unblock writes (TECH-WIRE-UP §1-3 → A1/A2/A3 automations) before building dependent UI pages | Active |
| 110 | 2026-05-03 | **TECH-WIRE-UP §2 verified: all 7 marketing Hub DBs accessible** via `GET /api/notion/databases` (returns 25 DBs total, all 7 hardcoded marketing IDs present: IG_PERFORMANCE/HOOK_PATTERN_LOG/TEMPLATE_LIBRARY/APPROVALS_LOG/WEEKLY_OPS_LOG/MARKETING_LOG/CAMPAIGN_DECISIONS). Caveat: this verifies the listing endpoint only — row-level queries through YD-render integration are a separate code path and the 04:30 open-loop on integration access remains unresolved until a row-fetch smoke test passes | §2 was a 2-min pre-flight gate before the A1/A2/A3 automation work. Listing-level success is enough to unblock §1 docs + spec authoring; row-level verify deferred until A1 backend-builder dispatch (which fetches rows directly) | Active |
| 108 | 2026-05-03 | **Marketing Notion cleanup pass — 8 DBs + Hub A page archived in single coordinated rename.** All 8 deprecated DBs renamed `[ARCHIVED] {original} — superseded 2026-05-03` with descriptions explaining replacement targets: Marketing Campaigns (old) `2e213a71…` (7 historical rows preserved), Campaign Decisions `36f79d19…`, Marketing Log `3ca17008…` (0 rows), Campaigns (Hub A) `25c247aa…1de`, Marketing Sprints (A) `25c247aa…ab5f`, YD Marketing Campaigns (A) `25c247aa…3c8765`, Campaigns & Series (A) `34e3a22a…`, Marketing Tracker `db0ca2ff…` (0 rows). Hub A page itself (`25c247aa-0d7b-811c-bc15-c1835457425b`) renamed to `[ARCHIVED] Marketing Operations Hub (Aug 2025)`. The "Campaign Hub" inline view in Hub A auto-resolves to the underlying archived `Campaigns (Hub A)` title — no separate rename needed. Audit confirmed no canonical DBs (Campaigns/Content Calendar/Decisions/Commitments/Focus Areas/Audiences/Sequences/Sessions Log/Projects/IG Performance/Hook Pattern Log/Template Library/Approvals Log/Weekly Ops Log/MCC Posts) had active inbound relations to any of the archived DBs. Created consolidated `docs/TECH-WIRE-UP.md` punch list as single entry-point for Wire/Nirmal. Updated `docs/marketing/NOTION-SETUP.md` § 1.1 (Do Not Wire) + `docs/MARKETING-ALIGNMENT.md` § 3 with ✅ completed status. Updated `docs/marketing/README.md` to point at TECH-WIRE-UP.md as start-here for tech | Tech is about to wire the dashboard (per EA-BRIEFING request earlier in session). Cleaning before wiring = Wire reads one canonical map; cleaning after = wire-then-patch as zombies leak through. All 8 archives are reversible (rename, not delete). The 7 historical campaign rows in Marketing Campaigns (old) stay queryable by URL (referenced from migrated Commitments YDS-262 → YDS-267 Notes). Marketing Tracker discovery during audit was the key surprise: it was a 0-row tech-built dashboard prototype with relations to canonical Commitments + Focus Area, NOT a true zombie — but empty, so safe to archive. Crawler filter rule documented two ways: (a) parent chain must include `307247aa-0d7b-8102-bfa0-f8a18d8809d9` (YDS Operating System v2) — excludes Hub A wholesale; (b) belt-and-braces: exclude any DB whose title starts with `[ARCHIVED]` — catches future archives without crawler config update | Active |
