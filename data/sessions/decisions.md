# Decision Register — Command Centre

| # | Date | Decision | Rationale | Status |
|---|------|----------|-----------|--------|
| 1 | 2026-03-21 | Only map spreadsheet IDs when evidence is exact — never guess strategy wiring | Incorrect wiring causes silent data corruption in dashboards | Active |
| 2 | 2026-03-21 | Keep Google Sheets as short-term source of truth (no immediate Notion migration) | Sheets already working, migration would stall feature dev | Active |
| 3 | 2026-03-21 | Use service-account auth for Sheets, not browser OAuth | Backend-only access, no user-facing Sheets flows needed | Active |
| 4 | 2026-03-21 | Redesign BMC as hybrid executive canvas (classic BMC + dashboard + detail drawer) | Plain BMC too static for CEO daily use; needs interactivity | Active |
| 5 | 2026-03-21 | Build marketing content features into Command Centre, not as separate MCC app | Reduces context-switching, leverages existing infrastructure | Active |
| 6 | 2026-04-08 | Fix frontend x-show→x-if for lazy module views | Alpine evaluates all x-show expressions on load including uninitialised lazy modules, causing 246 errors and empty panels; x-if removes from DOM until active | Pending |
| 7 | 2026-04-11 | Keep ERP as primary UI; wire to CC + Live Dashboard backends via API layer | ERP has better UI/UX (React+Tailwind, 78 pages); CC and LD already have the data infrastructure. Avoids duplicating backends. | Active |
| 7 | 2026-04-08 | Reconcile conflicting Stop hooks into single per-action format | Two Stop hooks had contradictory requirements (one summary row vs per-action rows) causing infinite loop at session end | Active |
| 8 | 2026-04-08 | Use esbuild for JS code splitting (not Vite/Webpack) | Zero-config, single dependency, 50ms builds, stays within Alpine.js stack — no framework change needed | Proposed |
| 9 | 2026-04-08 | Diagnosed live "data not loading" as auth-gate cookie encoding regression in commit 4979625; no code change yet | URL-encoding cookie on set + decodeURIComponent on read locks out users holding pre-deploy raw cookies until re-login. Vercel logs confirm all API endpoints return 200 after fresh login | Pending fix |
| 10 | 2026-04-08 | Added CRM_CONFIG_SPREADSHEET_ID and OPS_WAREHOUSE_SPREADSHEET_ID to Vercel Production env; deferred STRATEGY_SPREADSHEET_ID | Two were missing from prod but present in local .env causing silent data gaps. STRATEGY value doesn't exist locally — needs clarification from Dan before adding | Active |
| 11 | 2026-04-08 | Fix dashboard nav badge null-deref with optional chaining (not x-if wrapper) | Alpine evaluates x-text independently of x-show; old guard relied on x-show but x-text still threw. Optional chaining is smallest surface-area fix — one line in public/index.html:105 | Active |
| 12 | 2026-04-08 | No code change for "exports is not defined" error in notion-browser chunk — diagnosed as stale browser cache | Local + git-committed chunk is pure ESM with zero exports references. Dan's commit ffe0fef already fixed cache headers (app.js no-cache, hashed chunks immutable), so hard refresh resolves it and prevents recurrence | Active |
| 13 | 2026-04-10 | Bypass Supabase auth on yds-live-dashboard instagram pages using service-role admin client | Quick dev bypass to unblock instagram pages — RLS blocks unauthenticated queries. Proper user auth to be added before team rollout | Active |
| 14 | 2026-04-11 | Add x-api-key header bypass to CC auth-gate for ERP service-to-service calls | ERP proxy needs to call CC API without browser cookie session; API key is simplest secure pattern for internal service auth | Active |
| 15 | 2026-04-11 | Wrap ERP app root in ErrorBoundary component | No error boundaries existed — any component crash caused full white screen with no recovery option | Active |
| 16 | 2026-04-11 | Orders: Sheets for operations, Supabase for analytics — don't merge | Sheets has operational details (status, fulfillment, customer info); Supabase has financial/analytics (revenue, margins, attribution). Different purposes. | Active |
| 17 | 2026-04-11 | Campaigns: Notion/CC for management, Supabase for ad attribution only | Notion has rich campaign planning metadata; Supabase `dim_campaign_map` links UTMs to ad IDs. Complementary. | Active |
| 18 | 2026-04-11 | Instagram: Supabase/LD for ops page, Notion/CC for editorial calendar | Supabase has post-level operational data and performance views; Notion has editorial planning. Not duplicates. | Active |
| 19 | 2026-04-11 | Monorepo: merge ERP into CC repo, kill yds-erp and absorb yds-live-dashboard | 3 separate apps is too much to manage. ERP has the best UI (React, 78 pages). CC has the backend. LD's Supabase data can be called directly. One repo, one deploy. | Active |
| 20 | 2026-04-22 | Build System Map as single `/api/system-map` aggregate endpoint, not per-section routes | Simpler to reason about, agents/clients get atomic snapshot, 60s cache amortises fs scan cost across bursts | Active |
| 21 | 2026-04-22 | System Map exposes `idPresent` boolean for Sheets, never raw spreadsheet IDs | Read-only inventory endpoint must not leak secrets; test asserts no field >12 chars on sheet entries | Active |
| 22 | 2026-04-22 | Gitignore `data/schemas/*.json` captured API samples; commit only the rule + README | Samples contain live Notion page titles, Sheet row values, PR text — not safe to commit. Regeneration is one command (`node scripts/capture-schemas.js`) | Active |
| 23 | 2026-04-22 | Wire `.claude/rules/api-schemas.md` into backend-builder / tester / code-reviewer only, not all agents | Only those three write or review Notion/Sheets/GitHub parsing code. Adding to others would dilute setup without behaviour change — Alpine templates consume already-simplified keys | Active |
| 24 | 2026-04-22 | Rejected Next.js+React, Python SDKs, Clasp, and generic Notion-API-docs from a "add these to your stack" suggestion list | Next.js/React violates Alpine.js-only rule; Python splits Node-only server; Clasp is for `.gs` scripts inside Sheets (wrong pattern vs. Node `googleapis` SDK); generic Notion docs weaker than workspace-specific `notion-hub.md` | Active |
| 25 | 2026-04-23 | Dan ↔ Colin Queue uses existing dark theme, not the `#FAFAFA` light palette in the source spec | Light island inside a dark app causes context loss / eye strain; section hex values mapped cleanly onto existing `--amber` / `--red` / `--accent` / `--text-secondary` tokens with only one new `--amber-muted` added | Active |
| 26 | 2026-04-23 | Drop-create routes through the same approval gate as answer-update | Consistency with "all writes gated" rule beats marginal self-approval friction; auto-approve can be layered on later if it becomes annoying | Active |
| 27 | 2026-04-23 | Voice + attach on drop composer rendered as disabled stubs, not removed | Spec calls them out; preserving the surface fidelity avoids future re-layout when transcription infra lands. Tooltip reads "coming soon" so intent is clear | Active |
| 28 | 2026-04-23 | Paused Dan ↔ Colin pipeline before `ux-auditor` + `tester` — Dan tests UI manually first | Dan's feedback: UX audit on code he hasn't loaded is wasted compute; integration tests against a just-shipped view often test wrong things before manual validation | Active |
| 29 | 2026-04-23 | Drop `Read CLAUDE.md` from 7 agent pre-flight blocks — agents get CLAUDE.md via `claudeMd` system context already | Re-reading it was pure waste (~172 lines × every spawn). Pre-flight should only list reads that aren't in the auto-injected context: the primer, domain rules, and files the agent is about to edit | Active |
| 30 | 2026-04-23 | Document `SendMessage` continuation pattern in CLAUDE.md Dispatch rules instead of making it implicit convention | Orchestrators were respawning review agents per pipeline step, paying the pre-flight cost each time. Making the pattern explicit + giving a worked example (code-reviewer: spawn once for backend diff, SendMessage the frontend diff) ensures the first-spawn cost is amortised across the session | Active |
| 31 | 2026-04-23 | Corrected hardcoded Dan↔Colin Queue DB ID from `00969f07-8b4d-4c88-8a45-ec1e95b3bacb` to `43d71386-85a1-4582-a2dd-6d541bdcc5d3` | Original ID was mis-transcribed during yesterday's build — schema-verify step passed (a DB with matching schema did exist somewhere) but the ID itself pointed at nothing Rube could see. Real DB surfaced via Notion `/v1/search` query. Tightening rule: schema verification alone is insufficient — also run a live row query against the target ID before committing a hardcoded constant | Active |
| 32 | 2026-04-23 | Dan ↔ Colin v2 spec published as `dan-colin-queue-v2.md` (deltas-only) instead of rewriting v1 | v1 stays authoritative for unchanged specs (typography base, animations, layout grid, approval flow); v2 documents only the 10 improvements. Keeps spec surface small and diffable, frontend-builder reads both | Active |
| 33 | 2026-04-23 | Use Alpine `:class="dcAnswerFocused && 'dc-fab--hidden'"` to hide FAB on answer focus, not the spec's pure-CSS `:has()` selector | Spec assumed `.dc-fab` was descendant of `.dc-body` — it isn't (sibling), so `:has()` was a silent no-op. Alpine state binding works and gives future state-awareness (e.g. delayed reappear). Tightening rule: CSS-only selectors that assume DOM structure must be validated against the template before speccing | Active |
| 34 | 2026-04-23 | Resolved design-planner's 5 open questions with orchestrator defaults rather than blocking on Dan | Dan's standing "use your judgment" rule + questions were all low-stakes UX polish (scroll+expand, hash determinism, keyboard scope, cheatsheet placement, stale threshold). Defaults captured in the frontend-builder brief so audit trail is preserved; Dan can override on manual test | Active |
| 35 | 2026-04-23 | Identified repo collision: two unrelated apps (Alpine.js Colin/Notion v1.0.0 at `@yourdesignstore.in/.../market/yds-command-centre` and React ERP v2.0.0 at `~/Projects/yds-command-centre`) both pushing to GitHub `inkfishindia/yds-command-centre`. Recommended separation onto two repos; no push executed | Diverged at `Initial commit` (8bab433) on April 8 into two parallel lines that never merged. Pushes from either clone reject the other because file structures, package.json, and stacks are fundamentally incompatible. Cannot resolve via rebase/merge — must split into separate repos before today's Alpine.js work (`e672039` local-only) can go live anywhere | Proposed — pending Dan confirmation which app is canonical on Vercel |
| 36 | 2026-04-23 | From Dan's ERP reference app, ported three features into CC: (1) System Map "Views & Status" registry, (2) UTM Builder in Marketing Ops, (3) Activity Feed on Dashboard merging decisions + commitments + Dan↔Colin resolutions. Skipped Order/Production/Kanban (FactoryOS), AI Studio pages (covered by `pixel` + `/commission`), Google Workspace contexts (MCP), PPC Calculator (exists elsewhere), Portfolio drill-down (duplicates Focus Areas) | Picked on signal-to-effort: small high-value surfaces that filled real gaps (module honesty, campaign URL tagging, unified activity). Skipped either covered-already or FactoryOS-domain. Code-reviewer verdict PASS WITH NOTES across 14 gates, 784/784 tests | Active |
| 37 | 2026-04-23 | Diagnosed `/api/overview` 500 on Vercel production as ENOENT from `server/services/read-model-store.js` writing to read-only bundled filesystem (`server/data/read-models/*.json`). No fix committed yet — Dan to choose Option A (graceful ENOENT/EROFS swallow in `writeJson`, ~10 lines) vs Option B (redirect `DATA_DIR` to `/tmp` on Vercel, ephemeral cache only) | Postgres isn't wired on Vercel (`DATABASE_URL` not set → `db.isDatabaseEnabled()` returns false), so both fs write and DB fallback fail → catch block in `base.js:buildPersistedReadModel` re-throws. Same failure mode likely affects other read-models (dashboard, crm, ops, marketing-ops, tech-team, action-queue) but symptoms are masked by warm-lambda memory cache | Pending Dan choice |
| 38 | 2026-04-25 | Split Vercel build in `vercel.json` into `@vercel/static` for `public/**` + `@vercel/node` for `server.js`, with explicit routes for `/css/`, `/js/`, `/partials/`, favicons ahead of the catch-all `/(.*)` → server.js | Single `@vercel/node` builder was auto-transpiling browser bundles (`public/js/*.js`) from ESM to CJS, producing `require()` calls that the browser can't execute → blank page. Static builder serves the bundles untouched. Chosen over `"type":"module"` in package.json (would break CJS server) and renaming to `.mjs` (touches build script + every chunk reference) | Active |
| 39 | 2026-04-25 | Reuse existing Dan↔Colin Queue (`createDrop()` + `Section=📥 Drop, Owner=Colin, Status=Open`) as the offline-chat fallback inbox instead of building a new "Chat Inbox" DB | Backend service + endpoint + Notion DB schema already exist and are tested. `Owner` field already separates Colin's outbound questions to Dan from Dan's inbound drops to Colin. Net cost: zero new infra. If drops get noisy and the semantic mixing actually costs Colin time, easy to split later — the queue rows have enough metadata to migrate selectively | Active |
| 40 | 2026-04-25 | Defer per-DB structured forms (commitment form with owner+due+DoD pre-fields, decision form with rationale+status, etc.) for v1 of the offline chat composer — ship free-text textarea only | Per-DB forms add a maintenance surface (every form must stay in sync with Notion schemas) for marginal benefit, since Colin can structure free-text drops himself when he processes them. Promotes shipping over speculation. Will revisit if Colin's review of the drops surfaces "I keep manually re-asking Dan for the same metadata" patterns | Active |
| 41 | 2026-04-25 | Fix dan-colin view-leak with surgical `<template x-if>` → `<div x-show>` swap on dan-colin only (commit `ed520fb`); leave the same latent bug in the other 16 partial-loaded views unfixed | Dan-colin is the only partial with `position:fixed` element (the FAB) at root, so the only one whose leak is visible. Other 16 (team, factory, marketingOps, bmc, crm, techTeam, ops, registry, status, knowledge, projects, decisions, docs, system-map, claude-usage, commitments) have same latent bug but no symptom. Surgical fix avoids 16-view regression risk. Documented as known-but-deferred — convert reactively when any of those views grows position:fixed children | Active |
| 42 | 2026-04-28 | Daily Sales registers two tabs (`SALES_YTD` static `2026` + `SALES_CURRENT_MONTH` dynamic `"<MonthName> YYYY"`) instead of a single combined source | Reflects how Dan actually maintains the workbook (cumulative year tab + rolling monthly tab). Dynamic resolution lets the system auto-rotate on the 1st of each IST month with zero code change. Sparse `2026` tab still gets first-class treatment because Dan plans to populate it | Active |
| 43 | 2026-04-28 | Extended `sheets.js` `SHEET_REGISTRY` to support `sheetName` as either string or `(now) => string` function — first registry entry to use this pattern | Avoids hardcoding monthly tab names that break on Apr→May rollover. The function form is opt-in (existing 25+ static entries unchanged) and accepts an injectable `now` so tests stay deterministic and the IST-aware caller can pass a TZ-shifted Date. Cheap surface-area extension; one `resolveSheetName(entry, now)` helper threaded through `fetchSheet`/`getSheetHeaders`/`appendRow`/`updateRow` | Active |
| 44 | 2026-04-28 | YTD aggregation deduplicates the union of YTD + month tab rows (month wins on Order # collision) — chosen over "YTD reads YTD tab raw" | Reviewer-flagged blocker: with raw-YTD, the same Order # appearing in both tabs with different amounts caused YTD ≠ MTD ≠ trend30d, breaking the founder's mental model that "totals tie out." Dedup-then-aggregate makes all three views consistent. Test added covering the overlap case explicitly | Active |
| 45 | 2026-04-28 | All date math in daily-sales-service uses explicit `Date.UTC(y, m-1, d)` arithmetic, never `Date.parse()` or `new Date(string)` | `Date.parse` on `YYYY-MM-DD` is locale-sensitive across runtimes (UTC midnight on most, but historically not all) and `new Date('28-02-2026')` mis-parses to invalid or NaN. Explicit UTC math is portable and self-documenting. IST bucketing layered on top via `getISTNow()` (UTC + 5h30m, then read UTC components) — server runs UTC, business runs IST, "today" must mean Indian today | Active |
| 46 | 2026-04-28 | Daily-sales partial uses `<g x-show>` on SVG groups proactively, not `<template x-if>` — applied at build time, not retrospectively as on the 16 other views (#41) | Same lesson as decision #41 (view-leak from x-if on dynamically-injected partials), but applied before shipping. Reviewer caught three x-if instances in the SVG trend chart (gridLines, xLabels, hoverCols) — fixed with `<g x-show>` swap. Confirms the pattern is now part of the team's mental model: if it's a partial-loaded view, default to x-show on the conditional wrapper | Active |
| 47 | 2026-04-29 | Daily Sales YTD treats tab name as **Indian FY end-year** (`2026` = FY 25-26, ended Mar 2026; `2027` = FY 26-27, current). `SALES_YTD` resolves dynamically (`m >= 3 ? y+1 : y` against IST date); new `SALES_LAST_FY` registry entry resolves to `currentFYEndYear - 1`. Display label derives "FY 25-26" / "FY 26-27" from tab name. New `ytd.vsLastFY` delta uses **FY-ordinal like-for-like filter** (April 1 = FY day 1) so prior FY is sliced to today's FY-day-of-year, not calendar day. Auto-rotates April 1 IST | Static `SALES_YTD = '2026'` was reading last closed FY as if it were YTD. Dan operates on Indian FY for all reporting/statutory work — calendar-year tab is incoherent with the rest of his data model. Like-for-like filter is FY-ordinal not calendar-ordinal because day 1 of FY = April 1, so prior FY's "same period" must use the same ordinal anchor | Active — shipped with `vsLastFY` showing FY 26-27 vs FY 25-26 day-29: +160 orders (+16%), +₹18.4 lakh (+69.1%), AOV +₹1,223 (+45.8%). Dan dropped the monthly-tabs-only proposal in favor of consolidate-as-you-go (#48) |
| 48 | 2026-04-29 | Daily Sales workflow: **consolidate-as-you-go** (one rolling month tab + cumulative FY tab + monthly transfer ritual) over **monthly-tabs-only** (12 permanent tabs/year derived to FY total at runtime) | Dan flagged "becomes too many tabs / i would like to consolidate them as we go" against the monthly-tabs-only proposal. Trade-off accepted: ~2 min of manual work each month-end (copy April rows → append to `2027`, rename tab to May, clear data rows) in exchange for permanently 2 active tabs (current month + FY cumulative) plus archive. Backend's Order# dedup handles the transfer-window overlap safely (current-month wins on collision = freshest data wins). Same architecture supports either workflow — the choice is ergonomic, not technical | Active |
| 49 | 2026-04-29 | Skipped `code-reviewer` and `frontend-builder` agent dispatches for the FY frontend changes; main session edited the partial directly | Frontend changes were template-level: 3 stat cards gained delta blocks copied from existing MTD `vsLastMonthSameDate` pattern, period label gained an `x-text` binding, unavailable notice swapped raw tab name for display label. Risk surface is tiny — new bindings use `?.` chains and `x-show` guards consistent with the rest of the partial. Live API verification + 44/44 backend tests + Dan's pre-approval served as the gate. Documented as a precedent: when changes mirror an existing, reviewed pattern in the same file, agent dispatch overhead exceeds the value | Active — flag for retroactive review if anything misbehaves on Dan's browser test |
| 50 | 2026-04-29 | Refactor `daily-sales-service.js` from monolith into 6-module folder (`index`/`parse`/`filters`/`aggregations`/`data-quality`/`fy`) with normalized `Order` schema as the long-term contract. Backward-compat shim at old path | Phase 3 (Postgres mirror, Metabase, Observable Plot) requires a clean separation between row-source, parsing, filtering, aggregation, and data-quality. Without it, swapping Sheets→Postgres later means rewriting the service. With it, only `parse.js` (and a new `db-source.js`) change. Cost: one refactor pass; benefit: Phase 3 becomes additive not destructive. The `Order` schema is the contract — Phase 3 consumers (Metabase / direct API hits) all go through it | Active |
| 51 | 2026-04-29 | Realized filter (`Status ∈ {Delivered, Fulfilled, Partially Fulfilled, Fullfilled-typo}`) is **default-on** for financial aggregations (today/MTD/YTD/trend/topStates/concerns/mix-by-channel/orderType/payment/print), but **status mix breakdown stays unfiltered** | Dan's stated intent: "to get accurate realized sales figures." Default-on means honest numbers without an extra click. Status mix kept unfiltered because it IS the diagnostic — filtering it would hide the cancellation/draft volume that justified the filter in the first place. Reviewer flagged this divergence as subtle; left a code comment so future maintainers don't "fix" it. The realized filter accepts `'Fullfilled'` (with typo) because Dan's source data has 260 such rows | Active |
| 52 | 2026-04-29 | Filter cache is bounded LRU (50 entries) keyed by hashed FilterSpec + IST date bucket | With 8 filter dimensions the unbounded cache grew unboundedly per filter combination. 50 entries handles realistic CEO-usage patterns (~10–20 distinct slices) with headroom; on overflow, oldest evicts. 5-min TTL preserved. Map preserves insertion order so eviction is `_cacheMap.keys().next().value`. LRU-touch on read keeps frequently-accessed slices warm. Was a reviewer 🔴 blocker fixed in second backend pass | Active |
| 53 | 2026-04-29 | Backend emits **both** wide format `trend30d: [{date,orders,revenue}]` AND long format `trend30dLong: [{date,metric,value}]`. Frontend uses wide; long is reserved for Phase 3 chart-lib swap | Observable Plot / ECharts / Vega-Lite all consume long format natively. Emitting both costs nothing (derivation is trivial), keeps current frontend untouched, and means swapping the SVG sparkline for Plot is a one-line frontend data-source change with zero backend work. Comment in `_dsComputeTrendPaths()` explicitly references `trend30dLong` and the architecture doc so the swap is signposted | Active |
| 54 | 2026-04-29 | ESC key handling on the daily-sales partial uses Alpine's `@keydown.escape.window` directive on the wrapper element, NOT manual `document.addEventListener('keydown', ...)` | Reviewer flagged the manual-addEventListener pattern as a memory-leak risk on partial-reload. The Alpine directive ties the listener to the element's lifecycle — when the element ever leaves the DOM, Alpine removes the listener automatically. Idiomatic, zero bookkeeping. Same pattern should be applied to other partials that have global keydown handlers (none today, but the precedent is set). The wrapper's `display:contents` style means it doesn't render but still hosts events — necessary because the directive needs an element to attach to | Active |
| 55 | 2026-04-29 | Filter chip values render dynamically from `filters.available.<dimension>` (the API's discovered values), never hardcoded enums in the partial. `(unknown)` is filtered out of chip lists so it only appears in mix breakdowns | Found Order Type pills hardcoded to `B2C / B2B` — but actual schema is B2C / DS / Manual / Stores (no B2B). Hardcoded enum was wrong AND would silently fail to surface DS/Manual/Stores once Dan tags those rows. Pattern was already established for paymentMode pills but not extended; now both follow the same rule. Future filter dimensions (status, channel) should use the same dynamic-from-API pattern. Consequence: backend MUST keep `filters.available.<dimension>` populated and stable — it's a contract, not an internal detail | Active |
| 56 | 2026-04-29 | Sheet column normalizers (`parse.js:normalizeOrderType`, also any future `normalize*` helpers in this layer) are **data-defined, not enum-defined** — pass through any non-empty trimmed value, collapse only true blanks to `(unknown)`. Same rule for filter parsers (`filters.js:parseFilterSpec`) — accept any value verbatim, do case-insensitive comparison in `applyFilters` not at parse time | Found `normalizeOrderType` whitelisted only `'B2C'` / `'B2B'` and dumped everything else into `'(unknown)'`. Filter spec parser similarly whitelisted only `B2C/B2B/all`. The dashboard was reporting 647 "unknown" Order Type orders worth ₹36.87 lakh — but the source sheet had clean values: B2C 512 / DS 510 / Manual 102 / Stores 39. **Whole "data quality issue" was code, not data.** Lesson: enum whitelists in normalizers are an anti-pattern when the upstream source is human-edited (sheets, CRM forms, etc.) — categories evolve faster than code. Tighter rule: any normalizer that has an `if (val === 'X') return 'X'` ladder should be replaced by `return trimmed || '(unknown)'` unless there's a specific business reason to coerce/reject. Companion to #55 (frontend) — both layers must be data-defined for the contract to hold | Active — applied to `normalizeOrderType` + `parseFilterSpec.orderType`; should be retroactively applied to any other normalizer in `parse.js`/`filters.js` that has the same whitelist pattern (none found today, but watch for it) |
| 57 | 2026-04-29 | Sheet↔dashboard parity check is a permanent diagnostic at [scripts/probe-sheet-parity.js](scripts/probe-sheet-parity.js); `filters.available.*` is **intentionally scoped to current FY + current month tabs only**, NOT to the historical FY archive (`2026` tab is read for `vsLastFY` aggregation but does not feed `filters.available`) | Built the parity probe in response to "should we check all 3 tabs and all fields?" — generalizes the earlier targeted Order-Type probe to 7 columns × 3 tabs. Surfaces parser bugs, dedup bugs, and stale API entries. First run confirmed parser fixes (#56) work + revealed `Status="Lost"`, `Payment Mode="Cash"/"Cheque"`, and 130+ Shipping State variants live only in the closed `2026` archive tab. These show as 🔴 in the script BUT are correct behavior — historical-FY filtering is out of scope until Postgres + Phase 3. Documented the scope choice here so future parity runs are interpreted correctly: 🔴 from values existing only in `2026` tab = expected; 🔴 from values present in current-FY tabs but missing from API = real bug. Reusable verification prompt logged in handoff for paste-into-future-sessions. The script also exposed real data-hygiene issues (Tags column 200+ variants, Shipping State spelling variants, COMMMERCE typo) — those are source-data fixes, not code | Active — script is canonical for future verification; run after any change to `parse.js`/`filters.js`/`aggregations.js` |
| 58 | 2026-04-29 | All cross-period delta computations in `daily-sales/index.js` (`vsYesterday`, `vsLastMonthSameDate`, `vsLastFY`) MUST be apples-to-apples: both sides go through `applyFilters(orders, activeFilter)` before aggregating. Order-of-operations: compute `*AggFinal` (filtered) first, then derive deltas from those — never mix unfiltered `*Agg` with filtered `*AggFinal` | Original `vsLastFY` block ran BEFORE the filter pipeline, computing `delta(ytdAgg.X, lastFYAgg.X)` from gross orders. The displayed YTD strip used `ytdAggFinal.X` (filtered/realized). Result: shown YTD = 934/₹20.74L but delta = +160/+69.1% computed against gross 1159/₹45.12L. The math literally did not tie out (delta 160 ÷ (934−160) = 20.7%, but pct shown was 16% from the gross prior 999). Story flipped after fix: realized-vs-realized FY 26-27 vs FY 25-26 same period is **+25 orders / -17.7% rev / -19.9% AOV** — actual revenue contraction, not the +69.1% gross-vs-gross illusion. Lesson: any "current" and "prior" pair shown in the same UI card must come from aggregates that share filter state. Vigilant: when adding new period comparisons, place delta computation AFTER all filter applications and use the `*Final` aggregates exclusively | Active |
| 59 | 2026-04-29 | "Today" stat is the only card that exposes BOTH realized and gross — `today.allStatuses: { orders, revenue, aov, vsYesterday }` lives next to the existing realized fields. MTD and YTD intentionally remain realized-only (financial reports). Frontend shows the gross count as a quieter secondary line under each Today stat, with `x-show` guard `(allStatuses.X ?? 0) > (today.X ?? 0)` so the line vanishes when everything is realized | Realized filter excludes `Order Placed` (142 rows MTD, ~45% of recent orders) + `Processing` (65) statuses. Today's orders are mostly still in those pre-shipment statuses → `today.orders` shows 0 by lunchtime even when 12 orders came in (live data at fix time). For "Today" the user wants operational glance ("did orders happen?"); for MTD/YTD they want financial accuracy. Asymmetric exposure is the right call — financial reports stay clean of unrealized inflation, while the at-a-glance card surfaces operational reality. Wording: "12 placed today" / "₹61,369 placed today" / "₹5,114 avg placed" — "placed" maps to Dan's mental model better than accounting jargon ("unrealized"). Backend cost is ~zero (gross aggregates already computed; just surfacing them) | Active |
| 60 | 2026-04-29 | `vsLastMonthSameDate` source pool is `[...allOrders, ...lastFYOrders]` — concat without dedup, because the two pools are disjoint by date (current-FY tabs are April YYYY+1 onward; lastFY tab is April YYYY–March YYYY+1). The union exists specifically to handle the FY rollover case (April → March crosses FY boundary) without special-casing | Pre-fix `lastMonthMTDOrders` filtered only `allOrders`, which meant April's vs-last-month was permanently null/empty (March lives in the closed-FY archive `2026` tab). Could have special-cased `if (todayMonth === 'April') include lastFYOrders` but that's brittle and grows. Always-union approach: filter by date-range does the scoping; for non-April months, the lastFY rows simply don't match the date filter (no-op cost). For April, March rows from `2026` are included naturally. Trade-off: pools must remain disjoint by date — if someone misfiles April rows into the `2026` tab, this would double-count. Acceptable given month-wins dedup elsewhere catches misfiling at the parser level. Decision #57's scope rule (filters.available scoped to current FY only) still holds — `lastFYOrders` is used internally for lookback aggregations, never exposed to filter pickers | Active |
| 61 | 2026-04-29 | Daily Sales redesigned around batch-import reality (Yesterday-as-hero, v4) instead of pursuing more frequent imports (path B). v4 swaps Today→Yesterday in the hero strip, demotes Today to a small "↻ N placed since refresh" pill in a new sticky freshness banner, and stamps "Data through {yesterday.date}" prominently at top | Sheet is batch-imported once daily (evening/night IST). At 11am, Today is empty by design — but v3's Today-as-hero made that read as "broken." Path A (UI reframe) makes the dashboard truthful about what it is — a daily-cycle ops view — without depending on infra changes. Path B (every-2-4h imports) is a real upgrade but requires touching whoever owns the import script + Google API rate-limit risk + still has early-morning lag. Dan picked A. Backend cost minimal: `yesterday` block + `freshness.dataCutoff` field. Frontend adds `.ds-freshness-banner` (5 classes) and 3 date helpers. Reuses all v3 dual-metric stat-card classes | Active |
| 62 | 2026-04-29 | Banner "Data through {date}" anchors to `yesterday.date`, not `freshness.dataCutoff`, with fallback chain `yesterday.date \|\| dataCutoff` for defense | dataCutoff = "latest istDateKey with any orders" — which can be today (e.g. if today has 12 partial-day orders, dataCutoff = today). Yesterday hero card binds to `yesterday.date` (= today minus 1, always). If banner used dataCutoff, it would read "Data through Apr 29" while hero shows "Yesterday · Apr 28" — internally contradictory. Anchoring to `yesterday.date` keeps the user-facing cutoff stamp coherent with what's shown. dataCutoff stays in the API surface for future stale-import detection (when `dataCutoff < yesterday.date`, last night's import didn't run). MTD/YTD label appendices use the same fallback for consistency | Active |
| 63 | 2026-04-29 | Inline `style="color: var(--text-secondary); margin-left:2px"` on MTD/YTD delta-suffix spans (6 instances, pre-existing from v2) harmonized to `.ds-stat-delta-suffix` class as part of v4 work — closing a consistency gap I flagged but deferred during v3 review | Originally left out of v3 scope to keep that PR tight. v4 already touches the MTD/YTD label rows (adding "· through {date}" appendix), so the harmonization rides along at near-zero marginal cost. Tokens-only-via-classes is a hard constraint for the project; remaining inline styles in the partial are now scoped to filter-strip empty states + one mix-grid empty state, both structurally one-off. Pattern reinforced for future: when touching adjacent code, prefer to close known gaps rather than defer indefinitely | Active |
